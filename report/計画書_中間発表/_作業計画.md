# 結合レポート全面リライト — 作業計画

> このファイルは AI（Claude）向けの実行計画。`report/計画書_中間発表/` 内で
> 計画書・設計書を **50 ページ以内** に再構築するための工程表。
> 大規模作業のため複数セッションに分け、各フェーズ完了ごとに `/clear` する前提で書いてある。
> セッションを再開したら **まず本ファイルと §11 を読む**。

---

## 0. このドキュメントの位置づけ

- 編集対象は **`report/計画書_中間発表/` 配下のみ**。`report/計画書結合/` や他の `report/` 下は
  読み取り専用（参照元）として扱い、書き換えない。
- 本計画自体は `report/計画書_中間発表/_作業計画.md`。各フェーズ着手前にここを更新（進捗を反映）。
- 動的進捗は `.agent/activeContext.md` / `.agent/progress.md` にも従来どおり残す。

---

## 1. ゴールと完了条件

中間発表用の「計画書・設計書」を 1 本の `.tex` として `計画書_中間発表/` に再構築する。
2026-05-21 のユーザー方針変更により，自由演奏に加えて**ゲームモード**を設計へ織り込む（§4-2）。

完了条件（すべて満たして初めて完了）:

1. **PDF が 60 ページ未満**（表紙・目次・生成 AI 章を含めて。50 ページ以内が望ましいが，
   ゲームモード追加に伴い 50 超過は許容する。60 ページは超えない — 2026-05-21 ユーザー指示）。
2. **重複の解消** — Chapter 2・3 の「2 版収録」を 1 版に統合済み。
3. **設計章の簡素化** — 詳細を削り「プロジェクトの内容が伝わる粒度」。長い API 表・コード例は要点のみ。
4. **図表の書式崩れゼロ** — 行送り・列幅オーバーフロー・はみ出しがない。
5. **全図が draw.io ベース** — すべての図を `.drawio` → PNG で作成し `\includegraphics` で挿入する。
   **TikZ による作図は廃止**し，preamble から `tikz` 関連も除去する（2026-05-21 方針）。
6. **内部整合が取れている** — §9 のチェックリストを全消化（相互参照・用語・数値など）。
7. **ゲームモードの織り込み** — モード選択・目標テンポ提示・採点・スコア表示が計画書・設計書に
   反映され，自由演奏モードと一貫して記述されている。
8. `latexmk -lualatex` がローカルでエラー 0・警告（overfull 含む）を実害ないレベルまで解消してビルド成功。
9. `.tex` と生成 PDF・`fig/` 一式を同一コミットで push。

---

## 2. 入力資料

| 種別 | パス | 役割 |
|---|---|---|
| **主**結合 TeX | `report/計画書結合/23_計画書・設計書_24G1075.tex` | リライトのベース（3077 行） |
| 個人版 TeX | `report/計画書結合/plan_25G1021.tex` / `plan_25G1053.tex` | 補助参照（記述の取捨で参考にする程度） |
| 骨格テンプレ | `report/計画書_中間発表/plan_template.tex` | 課題指定の章立て（この構成を守る） |
| スタイル | `report/計画書_中間発表/hack-cover.sty` / `hack-fonts.sty` | 表紙・フォント。流用する |
| 結合元 PDF | `meetings/0429_3回/事前課題共有/arduino_塩澤.pdf` 他 5 本 | 原典。記述の事実確認に使う |
| レビュー要約 | `meetings/0429_3回/事前課題共有/review_*.md`（6 本） | 各 PDF の論点・誤りの要約。整合チェックの当たりに使う |
| FBS/PBS 元画像 | `meetings/0429_3回/事前課題共有/PBS/FBS/FBS.png` `PBS.png` | 既存ラスタ図。draw.io 再作成の下敷き |
| スケジュール元データ | `docs/` 配下 `team/schedule.md`・`team/roles.md` | WBS・ガント・役割分担の数値ソース |
| 仕様 SSOT | `.agent/architecture.md` / `.agent/api.md` | 設計記述の正典（プロトコル・SystemData 等） |

---

## 3. 現状分析（なぜ肥大・崩壊したか）

`23_計画書・設計書_24G1075.tex` は 3077 行・178 KB。問題は 3 つ。

### 3-1. Chapter 2・3 が「2 版収録」になっている（最大要因）

複数人のマージ済みドラフトをさらに連結したため、同じ節が 2 回出てくる。

- **Chapter 2「システムの基本設計」**
  - Block A（行 506–816）= 音楽/Processing 班マージ版。`機能一覧/システム構成図/操作IF/状態遷移図` を
    サブシステム別（`Processing音源サブシステム`／`楽譜進行・発音情報配信`）に記述。
  - Block B（行 817–1209）= Arduino/EMA 班マージ版。同じ 4 節を `要求一覧＋FBS`／`全体ブロック図` 等で
    体系的に記述（`\label` 付き）。
- **Chapter 3「システムの詳細設計」**
  - Block A（行 1214–1872）= 音楽/Processing 班版。`部品一覧/HW/SW/処理フロー/テスト`。
  - Block B（行 1873–3056）= Arduino/EMA 班版。同 5 節（`\label` 付き）。

→ **方針: Block B（`\label` 付き・体系的）を土台にし、Block A にしか無い事実
（Processing 音色・NOTE 受信フロー・ボイス管理など）だけを畳み込んで 1 版に統合**。残りは削除。
重複行マップは付録 A。

### 3-2. 図が壊れている／欠損している

- `\includegraphics` 参照は 5 点。うち **3 点が実体不在**:
  `WBS.PNG`・`arrow.png`（アローダイアグラム）・`ganto.png`（ガントチャート）はリポジトリに無い。
  `FBS.png`／`PBS.png` も `meetings/.../PBS/FBS/` にしか無く、`計画書結合/` 直下には無い（`\graphicspath` 未設定）。
- TikZ 図が 8 点（行 616/766/896/1134/1174/1287/1781/2092）。`\resizebox` で潰しているものがあり、
  レンダリング品質・はみ出しは要実測。

### 3-3. 表の書式崩れ

`longtable` 30・`table` 11。多くが手動 `p{...\textwidth}` 指定で、列幅合計が版面を超えてオーバーフロー
している疑いが濃い。`lstlisting` 4・`verbatim` 6 もあり、コード塊がページを食っている。

---

## 4. 確定した方針（ユーザー確認済み）

- **図の作成方式**: Claude が `.drawio`（mxGraph XML）を直接記述 → draw.io の CLI で PNG 書き出し →
  `\includegraphics` で挿入。draw.io 専用 MCP は使わない（未導入のため）。
- **図の作成スコープ**: **全図を draw.io 化**する。TikZ による作図は廃止し，preamble から
  `tikz`／`\usetikzlibrary` 関連も除去する（2026-05-21 方針変更。当初は「正常な TikZ は据え置き可」
  としていたが，作図方式を draw.io に一本化する）。
- **設計章の粒度**: 事細かに書かず大まか。プロジェクトの内容が伝われば可。長い API 表・コード例は削減。
- 章立ては `plan_template.tex`（課題指定）に従う: 計画書／基本設計／詳細設計／生成 AI 利用。

### 4-1. draw.io 書き出し環境（未整備 — Phase 0 で構築）

現状: draw.io デスクトップ無し・`drawio` CLI 無し・Docker はインストール済だが停止中・VS Code 拡張無し。

採用する書き出し手段（Phase 0 でどちらか確立）:

- **第 1 候補**: `brew install --cask drawio` でデスクトップを入れ、同梱 CLI で書き出す:
  `/Applications/draw.io.app/Contents/MacOS/draw.io --export --format png --scale 2 -o fig/x.png fig/x.drawio`
- **第 2 候補**: Docker 起動 → ヘッドレス書き出しイメージ（`rlespinasse/drawio-desktop-headless` 等）。

インストールはユーザー操作が要る場合がある。Phase 0 で `! brew install --cask drawio` を案内する。

### 4-2. ゲームモードの設計方針（2026-05-21 確定）

2026-05-21，ユーザー決定により**ゲームモード**を新要件として追加した。これに伴い Phase 4 を
4A〜4D に再分割する（§6）。設計骨子は次のとおり（Phase 4A〜4C の入力となる）。

- **2 モード構成**: 起動後にモードを選ぶ。①自由演奏モード（現行レポートの内容），
  ②ゲームモード（目標テンポを提示し，その通りに振れたかを採点・スコア表示）。
- **司令塔 = 指揮者ノード node\_01**: モード選択（ボタン／規定モーション）も採点計算も node\_01 が
  担う。指揮者ノードに PC は接続しない。モーションは非対象範囲の「複雑なジェスチャ認識」に
  踏み込まない単純動作に限定する。
- **目標テンポの提示 = メトロノームガイドのフェードアウト**: 曲頭はガイド（node\_01 の LED ＋
  楽器側 PC のクリック音）を目標テンポで刻み，曲の進行とともに徐々にフェードアウト，終盤はガイド
  なし。指揮者は記憶を頼りに維持する。
- **演奏テンポ = 指揮者の実振り BPM**: ゲームモードでも楽器は自由演奏と同じく実振り BPM で演奏する
  （CTRL の BPM 源は切り替えない）。目標テンポは採点の基準としてのみ使う。
- **目標テンポ = 1 曲 1 つ固定**: 曲を通して 1 つの目標 BPM を維持できたかを採点する。
- **採点**: 実際の振り間隔と目標拍間隔の誤差を集計する。メトロノームガイドが薄い／無い区間ほど
  重く配点する。
- **伝達**: CTRL パケットの予約 4 バイトに mode／目標 BPM／score を割り当て，指揮者→楽器→PC へ
  中継する。新パケット種別は増やさない。
- **表示 = 楽器側 PC（Processing）**: 初期画面（モード待機）・現在モード・目標テンポ・スコア・
  結果画面を Processing が描画する。指揮者ノードに画面が無いぶん，PC 側 UI を厚く記述する。

---

## 5. 成果物のファイル構成

`report/計画書_中間発表/` を以下に整える。

```
計画書_中間発表/
├── _作業計画.md             ← 本ファイル
├── 中間発表_計画書・設計書.tex  ← 新規・本体（ファイル名は §11 で最終決定）
├── hack-cover.sty            ← 流用
├── hack-fonts.sty            ← 流用
├── fig/                      ← 新規。図の置き場
│   ├── *.drawio              ← 編集可能ソース（コミットする）
│   └── *.png                 ← 書き出し画像（コミットする。ビルドはこれを参照）
└── （中間ファイル *.aux 等は .gitignore 済み。既存 plan_template.* は削除可）
```

- 本体 `.tex` の `\graphicspath{{fig/}}` を設定し、図参照を `fig/` 基準に統一。
- preamble は `23_` 版から移植しつつ未使用パッケージを整理。**TikZ は全廃**するため
  `tikz`／`\usetikzlibrary` 行を除去する（2026-05-21 方針。全図を draw.io 化するため）。
- 旧 `plan_template.{aux,fdb_latexmk,fls,log,out,pdf,toc,tex}` はビルド成果物・空骨格。
  本体完成後に不要なら削除（Phase 0 で判断、`plan_template.tex` の章立てだけは骨格として使う）。

---

## 6. フェーズ分割（各フェーズ＝1 `/clear` 単位）

各フェーズは「着手時に本ファイル＋指定資料を読む → 作業 → ビルド → コミット＆push →
activeContext/progress 更新」で閉じる。**フェーズをまたいで未コミットの状態を残さない**。

### Phase 0 — 環境構築と骨格作成 ✅（2026-05-20 完了）
- draw.io 書き出し環境を確立（§4-1）。確立できたら簡単な `.drawio` を 1 枚 PNG 化して疎通確認。
- ベースライン計測: `23_計画書・設計書_24G1075.tex` を欠損画像にダミーを当てて 1 回ビルドし、
  **現状ページ数を記録**（削減目標の基準）。
- 新本体 `.tex` を作成: preamble 移植＋整理、`plan_template.tex` の章立てで空骨格、`\graphicspath{{fig/}}`、
  表紙メタ情報（チーム 23・メンバー・日付）を記入。
- `fig/` 作成。空骨格で `latexmk -lualatex` がビルド成功することを確認。
- 完了条件: 空骨格 PDF がビルドできる／draw.io PNG 書き出しが 1 枚通る／ベースライン頁数が記録済み。

**✅ 完了結果（2026-05-20）**:
- draw.io 環境: `brew install --cask drawio` でデスクトップ導入。CLI は
  `/Applications/draw.io.app/Contents/MacOS/draw.io --export --format png --scale 2 --output 出力.png 入力.drawio`。
  日本語ラベル入り `.drawio` の PNG 書き出しが正常レンダリングで通ることを確認。
- **ベースライン頁数 = 97 ページ**（欠損 3 画像に実在画像を流用しビルド）。
  Overfull \hbox 54 件（うち 12 件が 20pt 超・最大 90.9pt）。目標 ≤50 に対し約半減が必要。
- 新本体 `report/計画書_中間発表/23_計画書・設計書.tex` を作成（骨格・11 ページ・Overfull 0）。
  preamble は `23_` 版から移植し graphicx 重複除去・`\usetikzlibrary` 統合・`\graphicspath{{fig/}}` 追加。
  章立ては `plan_template.tex` 準拠＋Ch4「生成AIの利用」。各 \section 直下に `% TODO(Phase N)` 記入。
- `fig/`（`.gitkeep` のみ）作成。
- PDF 運用: `.gitignore` に例外 `!report/計画書_中間発表/23_計画書・設計書.pdf` を追加し、
  本体は `.tex`＋PDF を同一コミットで管理（2026-05-20 ユーザー指示）。

### Phase 1 — Chapter 1「計画書」 ✅（2026-05-20 完了）
- 入力: `23_` 行 139–500、`docs/.../team/schedule.md`・`roles.md`、`review_プロジェクト見直_片岡.md`。
- 概要・スコープ・作業計画・V&V・資源・リスクを §7 のページ予算で簡潔化。
- 図 4 点を draw.io 化: FBS・PBS（元 PNG を下敷きに再描画）、WBS・アローダイアグラム・ガントチャート
  （欠損、スケジュール元データから新規作成）。WBS は表で足りるなら表のまま簡素化も可。
- 完了条件: Ch1 がビルド成功・予算内・図が崩れず表示。

**✅ 完了結果（2026-05-20）**:
- 図 4 点を draw.io 化（`fig/` に `.drawio`＋`.png` を両方コミット）: `fbs`・`pbs`（`meetings/.../PBS/FBS/`
  の実画像を下敷きに再描画。実画像は計画書旧記述より整理されており実画像準拠で作成）、`arrow`
  （アローダイアグラム・クリティカルパス赤強調・並行 240/250 を弧で表現）、`gantt`（13 週・5 フェーズ・
  5/20 発表マーカー入り）。書き出しは `draw.io --export --format png --scale 2 --crop --border 14`。
- WBS は **表**で簡素化（51 タスクのツリーは A4 で潰れるため。要素成果物粒度 13 行の `tabularx`）。
- Ch1 を全執筆: 概要（題目/目的/独自性）・スコープ（成果物/対象範囲/FBS/PBS/対応表）・作業計画
  （WBS 表/アロー図/役割分担表/ガント図）・V&V（MOE 表＋MOP/TPM 7 小節）・資源調達・リスク管理。
  片岡レビューの指摘（24G 勢の役割を WBS・役割表に明示、齋藤の表記統一）を反映。
- 参考文献（`thebibliography` 6 件）を Ch3 と Ch4 の間に追加。Ch1 が引用するのは teru/ninntenndo/
  ref:douki/ref:onkai の 4 件、ref:chien/ref:beat は Ch2・3 で引用予定。
- ビルド: `latexmk -lualatex` 成功・**21 ページ**（うち Ch1 は 11 ページ、予算 15 以内）・
  Overfull \hbox 0 件・未定義参照なし。MOE 表の `\multirow` 溢れは「MOE をヘッダ行にする」方式へ
  作り直して解消（WBS 表と同じスタイルに統一）。

### Phase 2 — Chapter 2「システムの基本設計」（重複解消）✅（2026-05-21 完了）
- 入力: `23_` 行 501–1209、`.agent/architecture.md`、`review_arduino_*.md`。
- Block A/B を 1 版に統合（Block B 土台＋A の固有事実）。機能一覧/構成図/操作IF/状態遷移を各 1 版へ。
- 図: 全体ブロック図・指揮者状態遷移・楽器状態遷移を整理。崩れていれば draw.io 化、正常なら TikZ 据え置き。
- 完了条件: 重複ゼロ・ビルド成功・予算内。

**✅ 完了結果（2026-05-21）**:
- **楽器台数を 4→5 に変更**（ユーザー決定）。最終構成 = 指揮者 1（XIAO ESP32-S3）＋楽器 5
  （金管 4＋ドラム 1、node\_02〜06、partId 0x02–0x06）＋PC 5（各楽器に 1 対 1）。Ch1 の台数表記
  5 箇所と §9 整合チェックリストも 4→5 に更新。
- システム構成図を draw.io 新規作成（`fig/system-overview.drawio`＋`.png`）。指揮者→楽器 5 台
  （WiFi UDP・実線）→PC 5 台（USB Serial・破線）の 3 列構成。Block A 行 613 / Block B 行 894 の
  2 枚の TikZ 構成図を 1 枚へ統合。
- Ch2 を全執筆（Block B 土台に Block A 固有事実を畳み込み）: §1 機能一覧（要求一覧表＋FBS F1〜F6）、
  §2 システム構成図（全体構成図＋ノード/パート対応表＋役割分担表＋データフロー＋EMA 方針）、
  §3 操作 IF（ユーザー操作＋LED 表＋通信メッセージ表）、§4 状態遷移図（指揮者・楽器の TikZ 図＋
  PC を含む 3 種の状態定義表）。指揮者・楽器の状態遷移は正常レンダリングのため TikZ 据え置き。
- 重複解消: Block A の「Processing 音源サブシステム」「楽譜進行・発音情報配信」の 2 版収録を解消。
  Block A 楽器状態遷移（SelfRun 含む旧版）は Block B の 3 状態版に統合し削除。

### Phase 3 — Chapter 3「システムの詳細設計」（重複解消＋最大の削減対象）✅（2026-05-21 完了）
- 入力: `23_` 行 1210–3056、`.agent/api.md`・`architecture.md`。
- Block A/B を統合。**最も強く削る**: 長い API 表（`tab:api` 他）・コード例（`lstlisting`/`verbatim`）は
  要点の散文＋小さな図表に置換。`ProjectConfig`/`SystemData` の全項目列挙は代表値のみ。
- 図: クラス図・処理フロー・HW 接続図を整理（崩れていれば draw.io 化）。
- 分量次第で Phase 3a（部品一覧〜HW〜SW）／3b（処理フロー〜テスト）に再分割してよい。
- 完了条件: 重複ゼロ・ビルド成功・予算内。

**✅ 完了結果（2026-05-21）**:
- Ch3 を全執筆（Block B 土台に Block A 固有事実を畳み込み，2 版収録を解消）。5 節構成:
  §1 部品一覧（HW 部品表＋SW 技術スタック表）、§2 ハードウェア設計（配線・ピン表＋
  ネットワーク構成の散文。接続図は Ch2 図\ref{fig:system-overview} を参照し新規図は作らず重複回避）、
  §3 ソフトウェア設計（ファイル構成ツリー＋クラス図＋モジュール表＋通信パケット 2 表＋
  ScoreEvent 表＋時刻同期の散文＋音色表）、§4 処理フロー（3 フェーズ表＋指揮者/楽器の
  applyPattern() フロー＋楽器処理フロー図）、§5 テスト設計（設計目標値表＋検証方向性＋
  仮値表）。
- **大幅削減**: Block B の apibox 約 15 個・`verbatim`・`lstlisting` を要点の散文＋小表へ圧縮。
  `IModule`/`ModuleTimer`/`OrcNetConfig`/`OrcNetData`/`NoteSenderConfig` 等の全項目列挙は削除し，
  モジュール責務は 1 表（`tab:module-list`）に集約。`ProjectConfig`/`SystemData` の全項目表も削除。
- 図 2 点を TikZ で新規作成（正常レンダリングを PDF で確認）: クラス継承図（`fig:class-diagram`）、
  楽器ノード処理フロー図（`fig:flow`，縦フロー・判定 3 分岐・戻り矢印付き）。HW 接続図は
  Ch2 と重複のため作らず。
- 整合: 楽器 5 台・partId 0x02–0x06（金管 0x02–0x05・ドラム 0x06）・node\_02〜06 で統一。
  `ScoreEvent` は実体準拠の 9 フィールド版（`sub*` は 1 行に集約）。`bpmQ8` は「BPM を 8 倍した
  整数（分解能 0.125 BPM）」表現。`.agent/api.md` の NOTE `partId` 範囲（本番想定）も 4 台/0x02–0x05
  → 5 台/0x02–0x06 へ追従（別コミット）。
- ビルド: `latexmk -lualatex` 成功・**45 ページ**（Ch3 は約 16 ページ）・Overfull/Underfull 0 件・
  未定義参照/引用なし。≤50 ページ内。次は Phase 4（生成 AI 章・全体整合・最終調整）。

### Phase 4 — ゲーム機能の織り込み・生成 AI 章・全体整合（4A〜4D に再分割）

2026-05-21 のゲームモード追加（§4-2）に伴い，当初 1 フェーズだった Phase 4 を 4 つに分割する。
各サブフェーズ＝1 `/clear` 単位。着手時に本ファイル §4-2・§6・§7 と，対象章の本文を読む。

#### Phase 4A — ゲーム機能を Ch2・Ch3 へ織り込む（中核）
- 入力: §4-2，本体 `.tex` の Ch2・Ch3 全文，`.agent/architecture.md`・`api.md`。
- Ch2: 章冒頭に 2 モード構成を宣言。要求一覧に R-5（モード選択）・R-6（テンポ採点）を追加。
  機能分解に F7「ゲーム進行・採点」（node\_01）を追加し，F5 に PC のゲーム UI を加筆。
  操作 IF にモード選択操作とメトロノームガイドを追記。状態遷移図を更新（指揮者に
  ModeSelect 状態，楽器・PC にモード反映）。
- Ch3: CTRL ペイロード表を改訂（予約 4 バイト → mode・targetBpmQ8・score 等）。指揮者の
  処理フローに採点ロジックを追加。メトロノームのフェード制御を記述。
- 図: 状態遷移 2 図を draw.io で新規作成（TikZ 廃止）。指揮者処理フローは 50 頁制約上まず散文で，
  必要なら draw.io 図を新設。
- 完了条件: ゲーム機能が Ch2・Ch3 に反映・ビルド成功。

**✅ 完了結果（2026-05-21）**:
- Ch2: 章冒頭に 2 モード（自由演奏／ゲーム）を宣言。要求一覧に R-5（モード選択）・R-6
  （テンポ維持の採点）を追加。機能分解に F7「ゲーム進行・採点」（node\_01）を追加し，F5 を
  「音響合成・演奏画面（PC）」へ改名してモード待機／スコア／結果画面の描画を加筆。操作 IF に
  「モード選択」（規定モーション・既定は自由演奏・大振り 1 回でゲーム・数秒静止で確定）と
  「メトロノームガイド」を追記。LED 表に node\_01 ModeSelect 行を追加。指揮者状態遷移に
  ModeSelect 状態を追加。楽器状態遷移はモードで変化しない旨を明記。
- Ch3: CTRL ペイロード表の予約 4 バイトを mode（1B・0=自由演奏/1=ゲーム）・targetBpmQ8
  （2B）・score（1B・0–100）へ割り当て。state を ModeSelect 挿入で 0–4 へ再採番。指揮者処理
  フローに「採点（ゲームのみ）」ステップを追加し，状態遷移を Idle→ModeSelect→… へ更新。
  新節「ゲームモードの採点とメトロノームガイド」を追加（メトロノームのフェード制御＝固定
  スケジュールで強度 1.0→0・通信不要，採点＝振り間隔と目標拍間隔の誤差をガイド強度で重み
  付け集計→0–100 へ写像）。
- 図: 状態遷移 2 図（`state-conductor`／`state-performer`）を draw.io 新規作成（`.drawio`＋
  `.png` をコミット）。本体の TikZ 状態遷移 2 ブロックを削除し `\includegraphics` へ置換。
  クラス図・処理フロー図の TikZ は Phase 4D まで据え置き（`tikz` パッケージは存置）。
- `.agent/api.md` は未修正（現行ファーム＝予約 4B のままが事実。報告書は将来設計を記述する
  立場であり差を許容する）。
- ビルド: `latexmk -lualatex` 成功・**45 ページ**・Overfull/Underfull 0・未定義参照/引用なし。
  次は Phase 4B（Processing 記述拡充・ゲーム UI 節新設）。

#### Phase 4B — Processing（PC 側）の記述拡充とゲーム UI ✅（2026-05-21 完了）
- 入力: 本体 `.tex` の Ch3 §音色合成節，`pc_app/test_v2/orchestra_resynth/`，`.agent/` 内 PC 関連。
- Ch3 の「音色合成（PC 側）」節を約 0.5 頁→約 2 頁へ増量（フレーム同期・Serial スレッド分離と
  受信キュー・Voice 管理・音色 JSON 外部化を小節化）。
- ゲーム UI 節を新設（初期画面＝モード待機，モード・目標テンポ表示，メトロノーム描画，
  スコア・結果画面）。PC の状態定義表をモード対応に更新。
- 完了条件: Processing 記述が拡充され・ビルド成功・予算内。

**✅ 完了結果（2026-05-21）**:
- Ch3 §3.3.6 を「音色合成（PC 側）」→「PC 側ソフトウェア（Processing）」へ改題・増量
  （`\label{sec:pc-software}`）。`orchestra_resynth.pde` 実体準拠で，フレーム同期（magic 走査・
  type 振り分け・ポート別同期状態）・Serial スレッド分離と受信キュー（ConcurrentLinkedQueue
  相当を散文化，発音処理は描画スレッドへ集約）・発音（Voice）管理（durationMs 自動消音・同時
  発音上限 24・最古強制リリース）・音色合成（倍音加算＋非調和性＋ビブラート/トレモロ＋
  整形ノイズ）・音色定義の外部化（JSON・instrumentId＝ファイル名昇順 index）を `\textbf` 小節化。
- Ch3 §3.3.7「画面とゲーム UI の設計」を新設（`\label{sec:pc-ui}`）。モード待機画面（ModeWait）・
  演奏画面（Playing，共通＋ゲーム）・メトロノーム表示・結果画面（Result）を小節化し，画面と
  表示要素を表 `tab:pc-screens` に集約。CTRL 中継（楽器ノード→専用 PC へ転送，PC が mode/
  targetBpmQ8/score を読む）を明記。図は新設せず表で対応（§8 の図最小化方針）。
- Ch2 PC 状態定義表 `tab:state-pc` をモード対応へ更新（Ready→ModeWait に改め，Result を追加。
  PortSelect/ModeWait/Playing/Result/Muted/Error の 6 状態）。表前の散文も CTRL 受信・モード
  切替に更新。
- 整合: §3.3.2 クラス図節の PC 記述末尾を `sec:pc-software`/`sec:pc-ui` 参照へ，Ch2 F5 の
  「ゲーム画面の詳細は第 N 章」を `\ref{sec:pc-ui}節` へ精緻化。`Ready` 残存参照ゼロ。
- ビルド `latexmk -lualatex` 成功・**47 ページ**・Overfull/Underfull 0・未定義参照/引用なし。
  次は Phase 4C（Ch1 へのゲーム反映・図の作り直し）。

#### Phase 4C — Ch1（計画書）への反映と図の作り直し ✅（2026-05-21 完了）
- 入力: 本体 `.tex` の Ch1 全文，`fig/` の各 `.drawio`。
- Ch1: 目的・成果物・対象範囲・リスクにゲームモードを反映。
- 図: `fbs.drawio` に「ゲーム進行・採点」機能群，`pbs.drawio` に「ゲーム UI」を追加して再描画。
  WBS 表にゲーム実装タスクを追加し，`arrow`・`gantt` を追従。全 PNG を再書き出し。
- 完了条件: Ch1 にゲーム機能が反映・図が最新・ビルド成功。

**✅ 完了結果（2026-05-21）**:
- Ch1: §目的に 2 モード構成（自由演奏／ゲーム）の段落を追加。§成果物の Processing 項に
  ゲーム UI（モード選択・目標テンポ・スコア・結果画面）を明記。§対象範囲に「2 モード構成と
  選択機能」「目標テンポの提示（メトロノームガイド）とテンポ維持精度の採点」の 2 項を追加。
  §FBS 説明を「4 機能群」→「5 機能群」へ改め，ゲーム進行（モード選択・目標テンポの提示・
  テンポ維持の採点）を加筆。§PBS 説明に「ゲーム進行モジュール」「ゲーム画面 UI」を加筆。
  FBS↔PBS 対応表にゲーム進行行を追加。§リスク管理に「ゲーム機能はコア機能の上に重ねる
  追加層であり，遅れても自由演奏モードのみで発表成立」の一文を追加。
- 図 4 点を作り直し（`.drawio`＋`.png` をコミット）:
  - `fbs`: 第 5 機能群「ゲーム進行」（葉＝モード選択／目標テンポの提示／テンポ維持の採点）を追加。
    ページ幅 1478→1830。
  - `pbs`: 指揮者ノードユニットに「ゲーム進行モジュール」，音響合成アプリに「ゲーム画面 UI」を
    追加（計画の「ゲーム UI のみ」から踏み込み，FBS↔PBS 対応の整合のため node\_01 側モジュールも
    追加）。ページ幅 1532→1768。
  - `arrow`: WBS 260 を「260 ゲーム機能（1.5 週）」の並行アーク（E3→E6，240/250 と同様）として追加。
    クリティカルパス（約 8 週）は不変。ページ高 430→470。
  - `gantt`: 12 行→13 行。「260 ゲーム機能」行を 250 の直下に追加（製造フェーズ・緑・5/27〜6/17，
    240/250 と並行）。フレーム・週線・凡例を追従。
- WBS 表: 製造フェーズに「260 ゲーム機能」行を追加（担当 塩澤・梅澤）。110 基本設計に「動作モード」を，
  250 Processing 実装の末尾を「演奏画面の描画」へ更新（モード待機・スコア・結果は 260 へ移動）。
  作業計画・ガント・リスクの散文も 260 を反映。
- ビルド `latexmk -lualatex` 成功・**47 ページ**・Overfull/Underfull 0・参照/引用の未定義なし
  （`undefined` は Hiragino W6 の bold series 警告 3 件のみで実害なし）。次は Phase 4D。
- 留意: FBS は 5 機能群化で横長になり `\linewidth` 配置で葉の文字がやや小さい。図は崩れず
  全体像は伝わるため据え置き。可読性を上げるなら横向き 1 ページ配置か縦レイアウト再設計。

#### Phase 4D — 生成 AI 章・全図 draw.io 化・全体整合・最終調整 ✅（2026-05-21 完了）
- 入力: `23_` 行 3058–3077，本体全文。
- Ch4「生成 AI の利用」を簡潔に執筆（予算 1 頁）。
- **残る TikZ 図（クラス継承図・楽器処理フロー図など，Phase 4A で draw.io 化しきれなかったもの）を
  全て draw.io 化**し，preamble から `tikz`／`\usetikzlibrary` を除去する。
- §9 整合チェックリストを全消化（前回レビュー指摘の同期誤差根拠・`ref:beat`・`gate`・CPU 時間・
  magic を含む。相互参照／用語／数値の総点検）。
- 最終ビルドで **総ページ数 60 未満を確認**（50 以内が望ましいが超過可，§1-1）。ゲーム＋Processing
  増量で +約 8 頁（45→約 53〜54 見込み）。53〜54 頁なら許容範囲なので無理な圧縮はしないが，冗長な
  箇所（Ch3 のパケット詳細・時刻同期の散文）があれば軽く詰める。60 に近づく場合のみ §7 予算に従い
  詳細設計→基本設計の順で削る。overfull/underfull を実害なきレベルへ。
- 完了条件: §1 の完了条件を全項目満たす。`.tex`＋PDF＋`fig/` を push。

**✅ 完了結果（2026-05-21）**:
- Ch4「生成 AI の利用」を執筆（`23_` 流用・微圧縮）。対話を通じた共同執筆・設計判断は著者が正・
  EMA／Arduino 制約に照らしたレビュー・誤りの責任は著者，の 3 段落。約 0.5 頁。
- TikZ 2 図を draw.io 化（`.drawio`＋`.png` 両コミット）。`class-diagram`＝IModule 抽象基底へ
  6 具象モジュールが継承（開三角矢印）。`flow`＝楽器処理フロー（縦フロー・判定 3 分岐・側枝 3・
  戻り矢印「次の拍へ」）。本体の TikZ 2 ブロックを `\includegraphics` へ置換し，preamble から
  `\usepackage{tikz}`／`\usetikzlibrary` を除去。**TikZ 全廃完了**（全 9 図が draw.io）。
- §9 整合チェックリスト全消化: ①同期誤差 20ms＝Ch1 結合 MOP を「数十 ms は気づかれにくい→
  ADR-0006 安全側 20ms」へ書き換え Ch3 設計目標値表と統一，②`ref:beat`＝Ch3 §3.4.4 楽譜進行の
  自己修復記述に `\cite{ref:beat}` 付与（参考文献 6 件すべて被引用），③NOTE `gate`＝パケット表で
  「常に 1。0 は将来拡張用予約・現設計は送らない」と注記，④CPU 時間＝「3 フェーズ合計 5ms 以内，
  入力はその一部 2ms」と制御周期基準で書き直し，⑤magic エンディアン＝「固定バイト列 0x4F 0x52・
  エンディアン対象外」と散文・ヘッダ表へ明記，⑥相互参照＝未定義 0 で解決，⑦用語＝node 名／
  モジュール名／CTRL・BEAT・NOTE 一貫，⑧数値＝制御周期 5ms・20Hz・200Hz・BPM 40–240・同期
  20ms 章間一致。ゲーム整合 3 項＝モード名一貫／CTRL 予約 4B（Ch2 通信メッセージ表にも mode・
  目標テンポ・score を明記し Ch3 パケット表と一致）／ModeSelect 等の状態名一貫。
- ビルド `latexmk -lualatex` 成功・**47 ページ**（60 未満・50 以内）・Overfull/Underfull 0・
  参照/引用の未定義なし（`undefined` 3 件は Hiragino W6 bold series 警告のみ・実害なし）。
- **結合レポート全面リライト完了**（Phase 0〜4D 全フェーズ完了。§1 完了条件を全項目充足）。

---

## 7. 章別リライト方針とページ予算

総計 ≤ 50。下表は目標（合計 47、予備 3）。超過時は「予備」→「詳細設計」→「基本設計」の順で削る。

> **進め方（2026-05-20 ユーザー方針）**: Phase 1〜3 の途中ビルドは多少 50 ページを超えてよい。
> 各 Phase でまず内容を書き切り、削減はユーザーと相談しながら進める。最終的な ≤50 ページは
> Phase 4 で確定させる（完了条件 §1-1）。各 Phase 末でページ数を報告し、超過幅を共有する。

| 章・節 | 目標頁 | 方針 |
|---|---:|---|
| 表紙・目次 | 3 | テンプレ流用 |
| **Ch1 概要**（題目/目的/独自性） | 2 | 議事録 9–10 章準拠。散文で簡潔に |
| **Ch1 スコープ**（成果物/対象範囲/FBS/PBS/対応） | 4 | FBS 図＋PBS 図。対応関係は小さな表 1 つ |
| **Ch1 作業計画**（WBS/アロー/役割/ガント） | 5 | WBS 図＋アロー図＋ガント図。役割分担は表 1 つ |
| **Ch1 V&V**（MOE/MOP/TPM） | 2 | 表中心。MOP の階層列挙は 1 表に集約 |
| **Ch1 資源・リスク** | 2 | 各 1 ページ目安 |
| **Ch2 機能一覧** | 3 | 要求一覧＋FBS。サブシステム別の重複表を 1 本化 |
| **Ch2 システム構成図** | 3 | 全体ブロック図 1 枚＋短い説明 |
| **Ch2 操作インターフェース設計** | 2 | LED 表示・ユーザー操作を各 1 表 |
| **Ch2 状態遷移図** | 4 | 指揮者・楽器の状態遷移図＋状態定義表 |
| **Ch3 部品一覧** | 2 | PBS 要素を 1〜2 表で |
| **Ch3 ハードウェア設計** | 3 | 配線・ピン表＋接続図 1 枚 |
| **Ch3 ソフトウェア設計** | 6 | クラス図＋パケット定義＋楽譜形式。API 表は要点のみ |
| **Ch3 処理フローの設計** | 3 | 3 フェーズモデル＋処理フロー図 |
| **Ch3 テストの設計** | 2 | 設計目標値表＋検証方針。未確定事項は短く |
| **Ch4 生成 AI の利用** | 1 | 既存 3058–3077 を流用・微圧縮 |
| 予備 | 3 | — |

> **2026-05-21 追補（ゲームモード）**: 上表は自由演奏のみの予算。ゲームモード追加で次の頁増を
> 見込む — Ch2 ＋約 1.5 頁（要求・機能分解・操作 IF・状態遷移），Ch3 ＋約 2 頁（CTRL 拡張・採点
> フロー・メトロノーム），Ch3 の Processing 節 ＋約 1.5 頁（増量），ゲーム UI 節 新設 ＋約 2 頁，
> Ch4 ＋約 1 頁。合計 +約 8 頁で，現状 45 頁→約 53〜54 頁。**ページ上限は 60 未満**（50 以内が
> 望ましいが超過可。2026-05-21 ユーザー指示）。53〜54 頁は許容範囲なので無理な圧縮は不要。60 に
> 近づく場合のみ「予備」→「詳細設計」→「基本設計」の順で削る。

---

## 8. 図の作り直し計画

本体 `23_計画書・設計書.tex`（現状）の図インベントリと方針。**2026-05-21 方針: 全図を
draw.io 化し，TikZ による作図は廃止する。**

| # | 図（label） | 現状 | 方針 |
|---|---|---|---|
| 1 | FBS 図（`fig:fbs`） | draw.io 済 | Phase 4C 完了（第 5 機能群「ゲーム進行」を追加） |
| 2 | PBS 図（`fig:pbs`） | draw.io 済 | Phase 4C 完了（ゲーム進行モジュール・ゲーム画面 UI を追加） |
| 3 | アローダイアグラム（`fig:arrow`） | draw.io 済 | Phase 4C 完了（260 ゲーム機能の並行アークを追加） |
| 4 | ガントチャート（`fig:gantt`） | draw.io 済 | Phase 4C 完了（260 ゲーム機能の行を追加） |
| 5 | システム全体構成図（`fig:system-overview`） | draw.io 済 | 据え置き（必要なら mode/score の流れを軽く加筆） |
| 6 | 指揮者ノード状態遷移（`fig:state-conductor`） | draw.io 済 | Phase 4A 完了（ModeSelect 状態を追加） |
| 7 | 楽器ノード状態遷移（`fig:state-performer`） | draw.io 済 | Phase 4A 完了（状態はモードで不変） |
| 8 | モジュールクラス継承図（`fig:class-diagram`） | **TikZ** | Phase 4D で **draw.io 化**（内容変更は小） |
| 9 | 楽器ノード処理フロー（`fig:flow`） | **TikZ** | Phase 4D で **draw.io 化**（内容変更は小） |

- TikZ 図は #6〜#9 の 4 点。すべて draw.io 化し，Phase 4D 完了時点で本体 `.tex` から
  `tikz`／`\usetikzlibrary` を除去する。
- ゲーム機能で図を新設するのは最小限にとどめる（50 頁制約）。指揮者の採点フローは散文＋小表を
  優先し，どうしても必要なときだけ draw.io 図を 1 枚足す。
- 各図は `fig/<name>.drawio`（ソース）と `fig/<name>.png`（書き出し）を **両方コミット**。
- 書き出し: `/Applications/draw.io.app/Contents/MacOS/draw.io --export --format png --scale 2
  --crop --border 14 --output fig/<name>.png fig/<name>.drawio`

---

## 9. 整合性チェックリスト（Phase 4D で全消化）

過去の夜間レビューと SSOT を踏まえた既知の不整合，および 2026-05-21 のレビューで判明した
本体 `.tex` 内の不整合。Phase 4D で全消化する。

### 既消化（Phase 1〜3 で対応済み）
- [x] **楽器ノード台数**: 指揮者 1＋楽器 5＝6 台で記述（Ch2 冒頭で 1 回宣言済み）。
- [x] **partId 範囲**: 5 楽器なので `0x02–0x06` で統一（金管 0x02–0x05・ドラム 0x06）。
- [x] **楽器名/音色**: 計画上の音色設計（金管系）で統一。
- [x] **`ScoreEvent` フィールド**: 実体準拠の 9 フィールド版を採用。
- [x] **`bpmQ8`**: 「BPM を 8 倍した整数（分解能 0.125 BPM）」と明記。

### Phase 4D で消化済み（2026-05-21）
- [x] **同期誤差 20 ms の根拠統一**: Ch1 結合テスト MOP を「数十 ms は気づかれにくい→ADR-0006 で
      合意した安全側 20 ms」へ書き換え，Ch3 設計目標値表の根拠と統一。
- [x] **`ref:beat` の引用**: Ch3 §3.4.4 楽譜進行の自己修復記述に `\cite{ref:beat}` を付与。
      参考文献 6 件すべてが被引用となった。
- [x] **NOTE の `gate`**: パケット表で「現設計では常に 1（NoteOn）。0（NoteOff）は将来拡張用の
      予約で現設計では送らない（消音は PC 側自動消音）」と注記し齟齬を解消。
- [x] **CPU 時間の根拠**: 設計目標値表を「3 フェーズ合計を制御周期 5 ms 以内に，入力フェーズは
      その一部として 2 ms 上限の目安」と書き直し。
- [x] **magic のエンディアン**: 「固定のバイト列 0x4F 0x52・数値ではないためエンディアン対象外」を
      散文とヘッダ表へ明記。多バイト数値のみリトルエンディアン，と区別。
- [x] **相互参照**: ビルドで未定義参照/引用 0。図の draw.io 化後も `\label`/`\ref` 全解決。
- [x] **用語の一貫**: node 名（`node_01`〜`node_06`）・モジュール名・CTRL/BEAT/NOTE を点検し一貫。
- [x] **数値の一貫**: 制御周期 5 ms・CTRL 20 Hz・IMU 200 Hz・BPM 40–240・同期誤差 20 ms が章を
      またいで一致することを確認。

### ゲーム機能の整合（Phase 4D で総点検済み）
- [x] モード名（自由演奏モード／ゲームモード）が全章で一貫。
- [x] CTRL 予約 4 バイト（mode・目標 BPM・score）が Ch2 通信メッセージ表・Ch3 パケット表で一致
      （Ch2 通信メッセージ表にもゲーム時の mode・目標テンポ・score を明記して整合）。
- [x] ゲーム関連の状態名（ModeSelect 等）が状態遷移図・状態定義表・本文で一致。

---

## 10. draw.io ワークフロー詳細

1. **作図**: Claude が `fig/<name>.drawio` を mxGraph XML として直接 Write する。
   - 文字は日本語可。フォント崩れ回避のため標準フォント指定。
   - A4 1 段に収まる縦横比（横長なら幅優先）。LaTeX 側は `width=\linewidth` 等で制御。
2. **書き出し**: §4-1 で確立した手段で PNG 化。`--scale 2`（高 DPI）推奨。`fig/<name>.png` に出力。
3. **挿入**: 本体 `.tex` で
   ```tex
   \begin{figure}[H]\centering
     \includegraphics[width=0.9\linewidth]{<name>.png}
     \caption{...}\label{fig:...}
   \end{figure}
   ```
4. **コミット**: `.drawio` と `.png` を両方 add（後から編集可能にするため XML を必ず残す）。
5. 図を直すときは `.drawio` を編集 → 再書き出し → 差し替え。`.png` を手で描き換えない。

---

## 11. セッション再開手順（`/clear` 後はここから）

1. 本ファイル `report/計画書_中間発表/_作業計画.md` と `.agent/activeContext.md` を読む。
2. activeContext の「現在の対象」で **どの Phase（4A〜4D）か** を確認。
3. §4-2（ゲーム方針）と §6 の該当サブフェーズ，§7・§8・§9 を読む。対象章の本文を Read。
4. 作業 → `latexmk -lualatex` でビルド成功確認 → ページ数を log で確認。
5. `/commit` でコミット（`[ドキュメント]` 種別）→ push。
6. `.agent/activeContext.md` 上書き・`progress.md` 追記・本ファイルの該当 Phase に ✅。

**確定済み事項（2026-05-21）**:
- 本体 `.tex` ファイル名 = `23_計画書・設計書.tex`。
- 楽器名/音色 = 金管系（金管 4＋ドラム）。
- ゲームモードを追加（§4-2）。司令塔 = node\_01，メトロノームガイドのフェードアウト方式，
  演奏テンポ = 実振り BPM，目標テンポ = 1 曲 1 つ固定。
- 全図 draw.io 化・TikZ 廃止（§8）。
- ページ上限 = 60 未満（50 以内が望ましいが超過可。§1-1）。

---

## 付録 A — 重複行マップ（`23_計画書・設計書_24G1075.tex`）

| Chapter | Block A（音楽/Processing 班）| Block B（Arduino/EMA 班・採用土台）|
|---|---|---|
| Ch1 計画書 | 行 139–500（重複なし。そのまま簡素化）||
| Ch2 基本設計 | 行 506–816 | 行 817–1209 |
| └ 機能一覧 | 506 | 817 (`sec:basic-functions`) |
| └ システム構成図 | 569 | 881 (`sec:basic-architecture`) |
| └ 操作IF設計 | 672 | 993 (`sec:basic-ui`) |
| └ 状態遷移図 | 721 | 1122 (`sec:basic-state`) |
| Ch3 詳細設計 | 行 1214–1872 | 行 1873–3056 |
| └ 部品一覧 | 1214 | 1873 (`sec:parts-list`) |
| └ ハードウェア設計 | 1219 | 1932 (`sec:hw-design`) |
| └ ソフトウェア設計 | 1308 | 1997 (`sec:sw-design`) |
| └ 処理フローの設計 | 1716 | 2467 (`sec:proc-flow`) |
| └ テストの設計 | 1834 | 2959 (`sec:test-design`) |
| Ch4 生成 AI | 行 3058–3077（重複なし）||

統合ルール: **Block B を土台に採用** し、Block A にしかない事実
（Processing 音色 `tab:harmonics`/`tab:adsr`/`tab:rhythm`、NOTE 受信/パケット検査/ボイス管理フロー、
Processing 側操作 IF・例外処理など）を Block B 側へ畳み込んでから Block A を削除する。
