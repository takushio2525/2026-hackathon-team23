# 現在の作業文脈

> このファイルは **毎ターン上書き**される動的文脈ファイル。
> 履歴は `progress.md` に追記する形で残す。

## 現在の対象

- **結合レポート Phase 5（本物画像到着を受けた最終調整）に着手**（2026-05-21）。
  成果物は `report/計画書_中間発表/23_計画書・設計書.tex`（47 ページ）。Phase 0〜4D の
  全面リライトは完了済み。
- 先輩がコミット `1c82ffc` で本物画像12点を `report/計画書結合/` に追加。差分を徹底調査し，
  ユーザーが4論点を決定済み（下記）。

## ユーザー決定（2026-05-21・Phase 5 の4論点）

1. 台数・ゲーム → 現行維持（5台・ゲームあり）。本物画像（4台・ゲームなし）は不採用。
2. FBS/PBS → 現行の技術版を維持。本物の素朴版には合わせない。
3. 参考文献 → 現行版を維持（元結合版へ差し替えない。中身は両版で同一）。
4. 状態遷移図 → 状態機械図2枚を廃し，動作シーケンス図へ作り替える。

→ FBS/PBS/arrow/gantt/system-overview・参考文献は**触らない**。届いた本物画像は本体 .tex へ
取り込まない（対比材料として確認しただけ）。

## 次の一手

Phase 5 の残作業は2つ（詳細は `_作業計画.md` §6-5 Phase 5）:
- **A. 状態遷移図のシーケンス図化**: §状態遷移図（`sec:basic-state`，本体 .tex 行651〜723）の
  状態機械図2枚（`state-conductor`・`state-performer`）を廃し，システム動作シーケンス図を
  draw.io 新規作成（`fig:sequence`）。指揮者 node\_01／楽器ノード×5／PC×5 の3ライフライン。
  起動→モード選択→キャリブレーション→演奏ループ→ゲーム採点→曲終了。状態定義表3つは残す。
  廃止する `state-conductor.{drawio,png}`・`state-performer.{drawio,png}` は `git rm`。
- **B. 整合性 中程度3件**: 中-1（Ch1「音量」→velocity 強弱）／中-2（WBS 240 を主旋律譜＋
  ドラム譜基準へ）／中-3（クラス図キャプションと本文の対応点検）。

着手したら本体 .tex 編集 → `latexmk -lualatex` → PDF 確認 → コミット。

## 現フェーズで Read すべき設計書

- Phase 5 全体方針: `report/計画書_中間発表/_作業計画.md` §6-5 Phase 5。
- A 着手時: 本体 .tex の §状態遷移図（行651〜723）と通信メッセージ表（行630〜648）。
  シーケンス図のメッセージは CTRL/BEAT/NOTE 準拠（Ch3 パケット定義節も確認）。
- B 着手時: 本体 .tex の Ch1 概要・目的（行88〜107），WBS 表（行248）。

## draw.io ワークフロー（再修正時の参照）

- 書き出し: `/Applications/draw.io.app/Contents/MacOS/draw.io --export --format png
  --scale 2 --crop --border 14 --output 出力.png 入力.drawio`
- `.drawio` と `.png` を両方コミットする。`.png` を手で描き換えない。

## ユーザーの好み

- 大規模作業は計画を作ってから着手。設計章は大まかに（内容が伝われば可）。
- 図は draw.io に一本化（TikZ 廃止済み）。表が読みやすければ図を表化してよい。
- 短い指示は行間を読み，前提のズレを感じたら着手前に指摘する。

## 既知の論点

- 本物画像（`計画書結合/` 配下）は全点「楽器4台・ゲームなし」で現行設計（5台・ゲームあり）と
  食い違うが，ユーザー判断で現行維持。本物画像は本体 .tex へ取り込まない。
- 状態遷移図をシーケンス図に差し替えると課題項目「状態遷移図」と形式がややズレるが，
  ユーザー決定。節名は「状態遷移図」のまま，状態定義表3つを残してフォローする。
- `.agent/api.md` の CTRL 予約 4B は未修正（現行ファームの事実）。ゲーム拡張は報告書側の将来設計。
- `.gitignore` に例外 `!report/計画書_中間発表/23_計画書・設計書.pdf` あり。本体 PDF はコミット対象。
