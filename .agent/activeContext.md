# 現在の作業文脈

> このファイルは **毎ターン上書き**される動的文脈ファイル。
> 履歴は `progress.md` に追記する形で残す。

## 現在の対象

- **結合レポート Phase 5 完了**（2026-05-21）。本物画像12点の到着を受けた最終調整
  （差分調査・状態遷移図のシーケンス図化・整合性修正）が完了。成果物は
  `report/計画書_中間発表/23_計画書・設計書.tex`（**49ページ**）。Phase 0〜5 全完了。

## Phase 5 で実施したこと（2026-05-21）

- 本物画像12点（`計画書結合/`，コミット `1c82ffc`）と現行 draw.io・両版 .tex・参考文献を
  徹底調査。ユーザー4論点決定: 台数ゲーム／FBS/PBS／参考文献は現行維持，状態遷移図のみ
  シーケンス図化（詳細は `_作業計画.md` §6-5）。
- §状態遷移図の状態機械図2枚を廃し，システム動作シーケンス図（`fig:sequence`）を新規作成。
  状態定義表3つ（指揮者・楽器・PC）は存置。
- 整合性 中3件を修正（Ch1「音量」→強弱／WBS 240 の粒度／クラス図キャプション）。
- ビルド成功・49ページ・Overfull/Underfull 0・未定義参照0。

## 次の一手

- Phase 5 完了。結合レポート全面リライト（Phase 0〜5）は全工程完結。
- 追加の修正指示があれば本体 .tex を編集 → `latexmk -lualatex` → PDF 確認 → コミット。
- 図は `fig/<name>.drawio` 編集 → draw.io CLI で PNG 再書き出し → 両方コミット。

## 現フェーズで Read すべき設計書

- 経緯・全方針: `report/計画書_中間発表/_作業計画.md`（Phase 0〜5 完了済み）。
- 本体修正時: `report/計画書_中間発表/23_計画書・設計書.tex`。

## draw.io ワークフロー（再修正時の参照）

- 書き出し: `/Applications/draw.io.app/Contents/MacOS/draw.io --export --format png
  --scale 2 --crop --border 14 --output 出力.png 入力.drawio`
- `.drawio` と `.png` を両方コミットする。`.png` を手で描き換えない。

## ユーザーの好み

- 大規模作業は計画を作ってから着手。設計章は大まかに（内容が伝われば可）。
- 図は draw.io に一本化（TikZ 廃止済み）。表が読みやすければ図を表化してよい。
- 短い指示は行間を読み，前提のズレを感じたら着手前に指摘する。

## 既知の論点

- 本物画像（`計画書結合/` 配下）は全点「楽器4台・ゲームなし」で現行設計（5台・ゲームあり）と
  食い違うが，ユーザー判断で現行維持。本物画像は本体 .tex へ取り込まない（対比材料）。
- 状態遷移図は課題項目だが，ユーザー決定でシーケンス図を主図とした。節名は「状態遷移図」の
  まま，状態定義表3つを存置してフォロー済み。
- `.agent/api.md` の CTRL 予約 4B は未修正（現行ファームの事実）。ゲーム拡張は報告書側の将来設計。
- `.gitignore` に例外 `!report/計画書_中間発表/23_計画書・設計書.pdf` あり。本体 PDF はコミット対象。
- 図インベントリは全10点 draw.io（`_作業計画.md` §8）。state-conductor/performer は Phase 5 で削除。
