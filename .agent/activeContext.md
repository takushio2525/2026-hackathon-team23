# 現在の作業文脈

> このファイルは **毎ターン上書き**される動的文脈ファイル。
> 履歴は `progress.md` に追記する形で残す。

## 現在の対象

- **2026-05-20**: 結合レポートの全面リライト（大規模・複数セッション）。
  先輩が結合した `report/計画書結合/23_計画書・設計書_24G1075.tex`（3077 行）が
  枚数肥大・図表崩れを起こしているため、`report/計画書_中間発表/` 配下に
  **50 ページ以内** の計画書・設計書を再構築する。
- **今セッションは計画立案のみ完了**。実行計画を
  `report/計画書_中間発表/_作業計画.md` に作成済み。
  以降は `/clear` しつつ Phase 0〜4 を順に実行する。

## 直近の観点

1. **実行計画の正典は `report/計画書_中間発表/_作業計画.md`**。
   セッション再開時は同ファイルの §11 と該当 Phase を読んでから着手する。
2. **肥大の主因＝Chapter 2・3 の「2 版収録」**。音楽/Processing 班マージ版（Block A）と
   Arduino/EMA 班マージ版（Block B）が同じ節を 2 回ずつ収録。Block B（`\label` 付き・体系的）を
   土台に統合し Block A 固有事実を畳み込む。重複行マップは計画書 付録 A。
3. **図の欠損**: `WBS.PNG`/`arrow.png`/`ganto.png` は実体不在。FBS/PBS も `meetings/` 配下のみ。
   方針確定: Claude が `.drawio`(XML) 直書き → draw.io CLI で PNG 書き出し → `\includegraphics`。
   変換スコープは欠損図＋崩れ図を優先（正常な TikZ は据え置き可）。
4. **draw.io 書き出し環境が未整備**（desktop/CLI 無し、Docker 停止中）。Phase 0 で
   `brew install --cask drawio` 等で構築する。
5. **編集対象は `report/計画書_中間発表/` 配下のみ**。`計画書結合/` 等は参照専用。

## 次の一手

- 本ターン: `_作業計画.md` 作成・activeContext/progress 更新をコミット＆push して終了。
- 次セッション（`/clear` 後）: **Phase 0** に着手。
  draw.io 書き出し環境構築 → `23_` のベースライン頁数計測 → 新本体 `.tex` 骨格作成。
- 着手時の未決定事項: 本体 `.tex` のファイル名／楽器音色を金管系 vs 実装(オルガン/フルート/ベル)
  のどちらで通すか（計画書 §9・§11 参照、ユーザー確認）。

## 現フェーズで Read すべき設計書

- **必ず最初に**: `report/計画書_中間発表/_作業計画.md`（実行計画の正典）
- Phase 実行時: 計画書 §6 の各 Phase「入力」欄に挙げた資料のみを Read（全文予防ロード禁止）
- 設計記述の正典: `.agent/architecture.md` / `.agent/api.md`

## ユーザーの今回の好み

- **大規模作業は計画を徹底的に作ってから着手**。今セッションは計画 md 作成のみと明示。
- **`/clear` しつつフェーズ単位で順次実行**する進め方を希望。
- 設計章は「事細かに書かず大まか、内容が伝われば可」。図は draw.io へ移行（MCP が理想だが未導入）。

## 既知の論点

- **楽器名/音色の不整合**: 結合元（Block A）は「金管/リズム」、実装 test_v2 は
  「オルガン/フルート/ベル」。中間発表時の事実に合わせるか計画上の設計で通すか、執筆時に要確認。
- **partId 範囲・楽器台数**: 本書は production 想定（指揮者 1＋楽器 4）で統一。
  `0x02–0x05`、test_v2 の 3 台輪唱は進捗として触れる程度。
- **整合チェックリスト**は計画書 §9 に集約。Phase 4 で全消化する。
