# 現在の作業文脈

> このファイルは **毎ターン上書き**される動的文脈ファイル。
> 履歴は `progress.md` に追記する形で残す。

## 現在の対象

- **結合レポート全面リライト**（大規模・複数セッション）。`report/計画書_中間発表/` 配下に
  **50 ページ以内** の計画書・設計書を再構築する。実行計画の正典は
  `report/計画書_中間発表/_作業計画.md`。
- **Phase 0・1・2・3 完了**（Phase 3 は 2026-05-21）。次は **Phase 4（生成 AI 章・全体整合・最終調整）**。

## 直近の観点

1. **正典は `report/計画書_中間発表/_作業計画.md`**。再開時は §11 と §6 Phase 4 を読む。
2. 現状: 本体 `report/計画書_中間発表/23_計画書・設計書.tex` は Ch1〜3 執筆済み・Ch4 は骨格のみ。
   `latexmk -lualatex` 成功・**45 ページ**（Ch1=11・Ch2≈8・Ch3≈16）・Overfull/Underfull 0・未定義参照なし。
3. **ベースライン = 97 ページ**。目標 ≤50。現在 45 ページ（**目標内**）。
4. **楽器台数は 5 で確定**。最終構成 = 指揮者 1（XIAO ESP32-S3）＋楽器 5（金管 4＋ドラム、
   node\_02〜06、partId 0x02–0x06）＋PC 5（各楽器に 1 対 1）。
5. draw.io 図ワークフロー: Claude が `.drawio` 直書き → CLI で書き出し
   `/Applications/draw.io.app/Contents/MacOS/draw.io --export --format png --scale 2 --crop --border 14
   --output 出力.png 入力.drawio`。`.drawio` と `.png` は両方 `fig/` に置きコミット。
6. **編集対象は `report/計画書_中間発表/` 配下のみ**。`計画書結合/` 等は参照専用。本体 PDF も同一コミット。
7. ページオフセット: **PDF ページ = 印刷ページ + 6**（表紙・目次が PDF 先頭 6 ページ）。

## 次の一手

- **Phase 4（生成 AI 章・全体整合・最終調整）に着手**。入力（_作業計画.md §6 Phase 4）:
  `23_計画書・設計書_24G1075.tex` 行 3058–3077、本体全文。
- Ch4「生成 AI の利用」を簡潔に執筆（予算 1 頁）。骨格 `\chapter{生成AIの利用}` の TODO を置換。
- **§9 整合チェックリストを全消化**: 残 [ ] は「相互参照（\ref/\label 全解決）」「用語の一貫
  （ノード名・モジュール名・CTRL/BEAT/NOTE）」「数値の一貫（同期誤差・BPM 範囲・周波数）」。
  Ch1〜3 全文を通読して検算する。
- 全図 PNG を最新 `.drawio` から再書き出し（fig/ の 5 図）。最終ビルドで **≤50 ページ確認**。
  現在 45 ページなので余裕あり。overfull/underfull を実害なきレベルへ。
- 完了条件 §1 を全項目満たして push。

## 現フェーズで Read すべき設計書

- **必ず最初に**: `report/計画書_中間発表/_作業計画.md`（§6 Phase 4・§9 整合チェックリスト・§11）
- Phase 4 実行時: `23_計画書・設計書_24G1075.tex` 行 3058–3077（生成 AI 章の原典）、本体 `.tex` 全文

## ユーザーの今回の好み

- **大規模作業は計画を作ってから着手**。`/clear` しつつフェーズ単位で順次実行。
- 設計章は「事細かに書かず大まか、内容が伝われば可」。図は draw.io へ移行（正常な TikZ は据え置き可）。
- 表が読みやすければ図を表化してよい。長い API 表・コード例は要点の散文＋小表へ圧縮。

## 既知の論点

- **楽器名/音色・台数は確定済み**（金管 4＋ドラム、楽器 5 台、partId 0x02–0x06）。
- **AGENTS.md・architecture.md・api.md**: 本番想定の楽器台数を 5・partId 0x02–0x06 へ追従済み。
- **ADR-0004**（5 台構成決定の旧記録）は当時の記録なので書き換えず、必要なら新 ADR で追補する
  （チーム判断）。Phase 4 でユーザーに 1 行確認してもよい。
- **整合チェックリスト**は計画書 §9 に集約。Phase 4 で残 3 項目（相互参照・用語・数値）を全消化する。
