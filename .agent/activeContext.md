# 現在の作業文脈

> このファイルは **毎ターン上書き**される動的文脈ファイル。
> 履歴は `progress.md` に追記する形で残す。

## 現在の対象

- **結合レポート全面リライト**（大規模・複数セッション）。`report/計画書_中間発表/` 配下に
  計画書・設計書を再構築する。実行計画の正典は `report/計画書_中間発表/_作業計画.md`。
- **Phase 0〜3 完了**。2026-05-21 に**ゲームモード追加**が決定し，Phase 4 を **4A〜4D に再分割**。
  次は **Phase 4A（ゲーム機能を Ch2・Ch3 へ織り込む）**。

## 直近の観点

1. **正典は `report/計画書_中間発表/_作業計画.md`**。再開時は §4-2（ゲーム方針）・§6 Phase 4A・
   §7・§8・§9 を読む。
2. 現状: 本体 `23_計画書・設計書.tex` は Ch1〜3 執筆済み・Ch4 は骨格のみ。`latexmk -lualatex`
   成功・**45 ページ**・Overfull/Underfull 0・未定義参照なし。
3. **ゲームモード追加**（_作業計画 §4-2）: 起動時にモード選択（自由演奏／ゲーム）。ゲームは
   目標テンポを提示し維持精度を採点。司令塔 = node\_01，メトロノームガイドのフェードアウト方式，
   演奏テンポ = 実振り BPM，目標テンポ = 1 曲固定，伝達 = CTRL 予約 4 バイト，表示 = 楽器側 PC。
4. **全図 draw.io 化・TikZ 廃止**（_作業計画 §8）。現状 TikZ は 4 図（state-conductor／
   state-performer／class-diagram／flow）。Phase 4A で状態遷移 2 図，Phase 4D で残り 2 図を
   draw.io 化し，最後に preamble の `tikz` を除去。
5. **ページ上限 = 60 未満**（50 以内が望ましいが超過可。2026-05-21 ユーザー指示）。ゲーム＋
   Processing 増量で 45→約 53〜54 頁見込み＝許容内。無理な圧縮は不要，60 に近づくときだけ削る。
6. draw.io 書き出し: `/Applications/draw.io.app/Contents/MacOS/draw.io --export --format png
   --scale 2 --crop --border 14 --output 出力.png 入力.drawio`。`.drawio` と `.png` を両方コミット。
7. 編集対象は `report/計画書_中間発表/` 配下のみ。ページオフセット: PDF 頁 = 印刷頁 + 6。

## 次の一手

- **Phase 4A に着手**（_作業計画 §6 Phase 4A）。本体 `.tex` の Ch2・Ch3 を Read。
- Ch2: 章冒頭に 2 モード宣言。要求一覧に R-5（モード選択）・R-6（テンポ採点）。機能分解に
  F7「ゲーム進行・採点」。操作 IF にモード選択操作・メトロノームガイド。状態遷移図を更新
  （指揮者に ModeSelect 状態）。
- Ch3: CTRL ペイロード表を改訂（予約 4 バイト → mode・targetBpmQ8・score）。指揮者の処理フローに
  採点ロジック。メトロノームのフェード制御。
- 図: 状態遷移 2 図（state-conductor／state-performer）を draw.io で新規作成（TikZ 廃止）。
- ビルド成功確認 → コミット → push → activeContext/progress 更新。

## 現フェーズで Read すべき設計書

- **必ず最初に**: `report/計画書_中間発表/_作業計画.md`（§4-2・§6 Phase 4A・§7・§8・§9）
- Phase 4A 実行時: 本体 `23_計画書・設計書.tex` の Ch2・Ch3 全文，`.agent/api.md`（CTRL パケット）

## ユーザーの今回の好み

- **大規模作業は計画を作ってから着手**。`/clear` しつつフェーズ単位で順次実行。
- 設計章は「事細かに書かず大まか，内容が伝われば可」。図は draw.io に一本化（TikZ 廃止）。
- 表が読みやすければ図を表化してよい。長い API 表・コード例は要点の散文＋小表へ圧縮。

## 既知の論点

- ゲームモードの設計骨子は _作業計画 §4-2 に確定記録済み。Phase 4A〜4C の入力。
- 前回レビューで判明した本体 `.tex` の不整合 6 件（同期誤差の根拠／`ref:beat` 未引用／NOTE
  `gate`／CPU 時間／magic エンディアン／相互参照・用語・数値）は _作業計画 §9 に集約。Phase 4D で消化。
- ADR-0004（5 台構成）は当時の記録なので書き換えない。
