# 現在の作業文脈

> このファイルは **毎ターン上書き**される動的文脈ファイル。
> 履歴は `progress.md` に追記する形で残す。

## 現在の対象

- 2026-06-24: production のドラムを56拍・14小節の4/4拍子に変更した。各小節の1・3拍目はキック、2・4拍目はスネアで、声部の入り（1/9/17/25拍目）はクラッシュとキックを同時に鳴らす。
- チューバは共通譜C4基準から2オクターブ下のC2相当へ戻した（Processing側の移調量 `-24`）。
- 金管4声とドラムに共通の `instrumentVolume=1.40` を掛け、従来より約+2.9 dB大きくした。`+` / `-` キーで演奏中に共通調整できる。
- `firmware/production/node_06` のPlatformIOビルド、`pc_app/production/orchestra_resynth` のProcessingビルドは成功した。実機へのuploadと聴感確認は未実施。

## 次の一手

- node_06へ新しいドラム譜を書き込み、全パート同時再生で音量・音割れ・チューバの聴こえ方を実機確認する。

## 現フェーズでReadすべき設計書

- プロトコル仕様: `.agent/api.md`
- ゲームモード設計: `.agent/test_v3-game-design.md`
- Git操作: `.agent/conventions.md`

## ユーザーの好み

- 大規模作業は計画を作ってから着手。短い指示は行間を読み，前提のズレを感じたら着手前に指摘。
- 実機未テストの `.ino` / `.cpp` は、変更後にコンパイルまで確認し、uploadはユーザー側で行う。
