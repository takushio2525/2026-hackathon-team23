# 現在の作業文脈

> このファイルは **毎ターン上書き**される動的文脈ファイル。
> 履歴は `progress.md` に追記する形で残す。

## 現在の対象

- **結合レポート最終チェック着手**（2026-05-21）。成果物は新版 `report/計画書_中間発表/
  23_計画書・設計書.tex`（47 ページ）。全面リライト Phase 0〜4D は完了済み。
- 本ターンで対比材料として元結合版 `report/計画書結合/23_計画書・設計書_24G1075.tex` を
  LuaLaTeX でコンパイル（**99 ページ**・Overfull 17 件・未定義参照 6 件）。
  元版の画像 12 点（FBS/PBS/WBS/arrow/ganto 等）は pull で取り込み済み。
- 最終チェックの具体観点は次のユーザー指示待ち。

## 直近の観点

1. 本体 `23_計画書・設計書.tex` は全 4 章（計画書／基本設計／詳細設計／生成 AI 利用）執筆済み。
   `latexmk -lualatex` 成功・**47 ページ**・Overfull/Underfull 0・参照/引用の未定義なし
   （`undefined` 3 件は Hiragino W6 bold series 警告のみ・実害なし）。
2. **全 9 図が draw.io ベース**（TikZ 全廃）。preamble に `tikz`／`\usetikzlibrary` は無い。
   図は `fig/*.drawio`（ソース）と `fig/*.png`（書き出し）を両方コミット済み。
3. ゲームモード（自由演奏／ゲームの 2 モード）は Ch1〜Ch3 へ全章織り込み済み。
4. §9 整合チェックリストは全項目消化済み（_作業計画 §9 参照）。
5. 編集対象は `report/計画書_中間発表/` 配下。ページオフセット: PDF 頁 = 印刷頁 + 6。

## 次の一手

- ユーザーの最終チェック指示を待つ。対象は新版 47 ページ `23_計画書・設計書.tex`。
- 元結合版（`計画書結合/`・99 ページ）は対比材料。新版が触る編集対象ではない。
- 新版を直すときは本体 `.tex` を編集 → `latexmk -lualatex` → PDF 確認 → コミット。
  図は `fig/<name>.drawio` を編集 → draw.io CLI で PNG 再書き出し → 両方コミット。

## 現フェーズで Read すべき設計書

- リライトの経緯・方針: `report/計画書_中間発表/_作業計画.md`（全フェーズ完了済み）
- 本体修正時: `report/計画書_中間発表/23_計画書・設計書.tex`

## draw.io ワークフロー（再修正時の参照）

- 書き出し: `/Applications/draw.io.app/Contents/MacOS/draw.io --export --format png
  --scale 2 --crop --border 14 --output 出力.png 入力.drawio`
- `.drawio` と `.png` を両方コミットする。`.png` を手で描き換えない。

## ユーザーの好み

- 大規模作業は計画を作ってから着手。設計章は大まかに（内容が伝われば可）。
- 図は draw.io に一本化（TikZ 廃止）。表が読みやすければ図を表化してよい。
- 長い API 表・コード例は要点の散文＋小表へ圧縮。

## 既知の論点

- FBS は 5 機能群化で横長になり葉の文字がやや小さい。図は崩れず据え置き。
  可読性を上げるなら横向き 1 ページ配置か縦レイアウト再設計。
- `.agent/api.md` の CTRL 予約 4B は未修正（現行ファームの事実）。ゲーム拡張は報告書側の将来設計。
- ADR-0004（5 台構成）は当時の記録なので書き換えない。
- `.gitignore` に例外 `!report/計画書_中間発表/23_計画書・設計書.pdf` を追加済み。本体 PDF はコミット対象。
