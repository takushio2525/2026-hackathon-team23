# 夜間レビューレポート 2026-05-22

対象リポジトリ: `2026-hackathon-team23`（HEAD `23c2d72`）
直近7日間はコード（`.ino`/`.cpp`/`.pde`）の変更ゼロ。差分は LaTeX 結合レポート・`.agent/`・
`docs/`・`fig/` に集中している。

> 注記: 本環境には Docker / ローカル LuaLaTeX（Hiragino フォント）が無いため、
> `23_計画書・設計書.tex` の独立再ビルドは未実施。ページ数・Overfull/未定義参照の
> 状態は `.agent/activeContext.md`・`progress.md` の記録（53頁・Overfull/Underfull 0・
> 未定義参照0）に依拠している。**要・ユーザー手元での最終再ビルド確認**。

---

## 1. ドキュメント同期チェック

### 1.1 production の楽器台数・partId 範囲が文書間／コード間で不一致 — 重要度: Med
最新の方針（ADR-0004 改訂・結合レポート）は「production＝楽器5台（金管4＋ドラム）」だが、
一部のコード／docs が旧4台のまま取り残されている。

- 5台（`0x02〜0x06`）と記述: `AGENTS.md:8`・`AGENTS.md:104`、`.agent/api.md:50`、
  `.agent/architecture.md:7`、`report/計画書_中間発表/23_計画書・設計書.tex:530`
  （表 `tab:part-map` で `node_06`/`0x06`/ドラムを明記）
- 4台（`0x02-0x05`）のまま: `firmware/test_v2/common/lib/OrcProtocol/OrcProtocol.h:53`
  （コメント `production 想定は 0x02-0x05`）、
  `docs/src/content/docs/architecture/protocol.md:136`（`production 想定は 0x02〜0x05（楽器 4 台）`）
- 実体: `firmware/production/` のフォルダは `node_01〜05` のみ（楽器は `node_02〜05` の4台、
  `node_06` 未作成）。`.agent/architecture.md:8` 自身がこの未作成を明記している。

修正案: 5台を正とするなら `OrcProtocol.h:53` のコメントと `protocol.md:136` を
「`0x02〜0x06`／楽器5台」へ統一。ただし `OrcProtocol.h` は test_v2 実機ファームのため、
コメントのみの修正でも「実機未テストコードへの介入禁止」ルールに留意（コメント変更は
無害だがユーザー判断を推奨）。あわせて `firmware/production/` に `node_06` 雛形を足すか、
「雛形は4台どまり・5台目は後日」と明記するかの方針決定が要る。

### 1.2 `beatNo` のオリジンが docs と api.md で矛盾 — 重要度: Med
- `.agent/api.md:42`: 「拍番号（**1 オリジン**…最初に飛ぶ BEAT は `beatNo=1`）」＋詳細な根拠
- `report/計画書_中間発表/23_計画書・設計書.tex:976`: 「`beatNo` は1始まり」、`idx = beatNo-1-headRestBeats`
- `docs/src/content/docs/architecture/protocol.md:107`: 「拍番号（**0 オリジン**、巻き戻しなしの単調増加）」

`api.md` は自らを SSOT と明記（`.agent/api.md:3`）し report とも一致。`protocol.md:107` が
旧記述で誤り。2026-05-15 の夜間レビュー対応（progress.md）で `beatNo` を1オリジンへ
統一した際の取り残しと思われる。

修正案: `protocol.md:107` を「1 オリジン」へ修正。同ファイル内に楽譜 index 算出式があれば
`beatNo-1-headRestBeats` と整合しているか併せて確認する。

### 1.3 AGENTS.md「CI が全ノードをビルド」は不正確 — 重要度: Med
- `AGENTS.md:78`: 「CI（`.github/workflows/pio-build.yml`）が `firmware/` 変更時に
  全ノードを自動ビルドする」
- 実体 `.github/workflows/pio-build.yml:18-28`: ビルド matrix は `test_v1/node_01〜04` と
  `test_v2/node_01〜04` の8件のみ。`firmware/production/**` はビルド対象外。
  push/PR トリガの paths は `firmware/**` のため production 変更で CI 自体は起動するが、
  production ノードはビルドされない（「全ノード」は事実と異なる）。

修正案: `AGENTS.md:78` を「test_v1・test_v2 の全ノードを自動ビルドする（production は
対象外）」に改める。または production をビルド対象に追加する（ただし `production/node_02〜05`
は空雛形、`production/node_01/platformio.ini` の board も暫定のため、現状ではビルドが
通らない見込み → 雛形が実装されるまでは文言修正が現実的）。

### 1.4 ゲームモード protocol 拡張が api.md（SSOT）に反映されていない — 重要度: Low
結合レポートはゲームモード設計として CTRL の `reserved[4]` を `mode`/`targetBpmQ8`/`score`
へ割り当て、`state` に `4=ModeSelect` を追加している（`23_計画書・設計書.tex:926-929`、
test_v2 では予約ゼロ埋めと `.tex:948` で明記）。一方 `.agent/api.md:33,43` は CTRL を
`reserved[4]`・`state` 0〜3 のままで、この設計拡張への言及がない。

修正案: `api.md` は現行 test_v2 の SSOT である一方、設計（report）と乖離しはじめている。
`api.md` に「結合レポート設計では `reserved[4]` を `mode`/`targetBpmQ8`/`score` へ割当・
`state` に `4=ModeSelect` を追加（test_v2 未実装）」の注記を1行足すと SSOT と設計が
1か所で追える。`activeContext.md`「CTRL state は現行ファーム整合で…4=ModeSelect」も、
「現行ファーム整合」と書きつつ現行に無い `4` を含む点が紛らわしいので表現を見直したい。

### 1.5 図インベントリ「全10点」表記の紛らわしさ — 重要度: Low
`report/計画書_中間発表/_作業計画.md` §8 と `.agent/activeContext.md` が「図は全10点が
draw.io」と記すが、`#6`/`#7`（状態遷移図）は Phase 5 で廃止済み。実体は本文
`\includegraphics` 8点・`fig/*.png` 8点。番号体系上 `#1〜#10` を割り当てた結果の欠番で
誤りではないが、実ファイル数と紛らわしい。

修正案: 「実体8点（#6/#7 は廃止・欠番）」へ表現を更新。優先度は低い。

---

## 2. バグ・矛盾レビュー

直近7日間でコード変更が無いため、型エラー・lint・テスト失敗・未使用 import 等の新規
発生はなし。CI（pio-build）も firmware 無変更のため影響なし。以下は既知の構成上の懸念。

### 2.1 production/node_01 の board 設定が指揮者ノードと不一致 — 重要度: Low（既知・暫定）
- `firmware/production/node_01/platformio.ini`: `board = uno_r4_wifi`
- `AGENTS.md:101-102`: 指揮者 `node_01` は **XIAO ESP32-S3 Sense**

`platformio.ini` 冒頭コメントが「ボード型番は暫定」と明記しており、production は意図的に
素テンプレ。過去の夜間レビュー（2026-05-15/17/18）でも保留扱いの累積指摘。実機ビルド
検証を伴うため Claude 起点での変更はせず、production 実装着手時にユーザーが修正する想定。

### 2.2 ドキュメント間の矛盾（章1の再掲）— 重要度: Med
コードの論理バグではないが、章1.1（partId）・1.2（beatNo オリジン）は明確な「矛盾」で
あり、提出物・公開 docs に残ると読者を誤誘導する。章1の修正案を参照。

### セキュリティ
新規コードなし。埋め込みトークン・injection 余地の新規発生なし。SoftAP パスワード
`orchestra2026` は平文ハードコード（`23_計画書・設計書.tex:819` 等）だが、過去レビューで
「会場 LAN 非依存の意図的設計・影響 Low」と整理済み。新たな所見なし。

---

## 3. 改善・新機能アイデア

- **partId／台数のドリフト検出 CI lint**: docs/コードの partId 範囲・楽器台数を grep で
  突き合わせる軽い CI チェック。なぜ必要か: 同じ partId/台数の不一致が夜間レビューで
  繰り返し検出されている（章1.1）。工数 M／優先度 Low。
- **api.md を設計拡張も含む単一 SSOT に**: ゲームモードの protocol 拡張節を api.md に
  追記（章1.4）。なぜ必要か: report と api.md が別々に protocol を記述し乖離し始めている。
  工数 S／優先度 Med。
- **production を CI ビルド対象に（雛形実装後）**: production ノードが実装され次第
  matrix へ追加。なぜ必要か: production の board 不一致など累積指摘の根因は CI が
  production を見ていないこと。工数 S／優先度 Low（雛形が実装されるまで保留）。
- **`firmware/production/node_06` の方針確定**: 5台構成決定に合わせ node_06 雛形を足すか、
  4台どまりと明記するか。なぜ必要か: `architecture.md:8` が未作成を明示したまま放置されている。
  工数 S／優先度 Low。

---

## 4. 直近作業のディープレビュー（過去7日）

過去7日（2026-05-15〜22）のコミットは約30件。トピックは2件に集約される。

### トピック1: 結合レポート（計画書・設計書）の全面リライト＋ゲームモード追加＋最終チェック

**やったこと（時系列）**
- Phase 0〜1（`a82fa88`〜`6430c27`）: draw.io CLI 環境構築・骨格作成、Ch1「計画書」執筆。
  WBS を表化、図4点（FBS/PBS/arrow/gantt）を draw.io 化、参考文献6件を整備。
- Phase 2〜3（`d387073`〜`b9ec336`）: Ch2「基本設計」・Ch3「詳細設計」。楽器台数を4→5台へ
  変更、Block A/B の重複収録を解消、system-overview 図を新規作成。
- Phase 4A〜4D（`3e15fdc`〜`56a2833`）: 2モード（自由演奏／ゲーム）設計を Ch1〜3 へ織り込み、
  Processing 記述を拡充、生成AI 章を追加、TikZ を全廃して全図 draw.io 化。
- Phase 5（`1c82ffc`〜`5c40eef`）＋ガント整合（`4f997f1`）: 状態機械図2枚を動作シーケンス図へ
  統合、ガントをアロー図・WBS・授業日程へ整合。
- 最終チェック（`eda3870`〜`23c2d72`）: 提出前チェックで指摘15件を起票→即修正5件→残10件→
  最後の高③・中⑨ まで対応し全15件決着。句点を「。→．」へ一括統一（`19edb91`）。

**完了度**: `.tex` に TODO/FIXME/WIP の残骸なし。`_作業計画.md` §6-6（Phase 6）で指摘15件
すべて「決着／完了」。`activeContext.md` も「最終チェック対応は完了。次の指示待ち」。
ビルド53頁・Overfull/Underfull 0・未定義参照0 は記録上の値で、本環境では再ビルド未実施
（要・手元確認）。完了度は高い。

**設計判断**:
- 図を draw.io XML 直書き→CLI で PNG 化。draw.io MCP 不採用・TikZ 全廃。保守性と
  レンダリング崩れ回避の観点で素直で妥当。
- 状態遷移図を動作シーケンス図へ統合（ユーザー決定）。状態定義は表で存置。妥当。ただし
  節名は「状態遷移図」のままシーケンス図を主図にした（低⑪で現状維持と決着済み）—
  節名と主図の種別が一致せず読者が一瞬戸惑う可能性は残る。意図的決着なので所見のみ。
- production を楽器5台で記述。設計書が実装に先行するのは正当だが、`OrcProtocol.h`・
  `protocol.md` の追従漏れが残った（章1.1）。
- ページ上限を50→60未満へ緩和（ユーザー指示）。53頁に着地。許容範囲。

**品質（コード系）**: 7日間でコード変更なし（`.tex`/`.md`/`.drawio`/`.png` のみ）。型・lint・
テスト・カバレッジに該当する所見なし。

**コンテンツ品質（レポート）**:
- 章立ては一貫（Ch1 計画→Ch2 基本設計→Ch3 詳細設計→参考文献→Ch4 生成AI）。
- 参考文献6件（`teru`/`ninntenndo`/`ref:douki`/`ref:chien`/`ref:beat`/`ref:onkai`）は
  すべて本文で `\cite` 被引用済み（行135・136・371・495・1185・378）。
- 用語・数値の整合は良好。partId・ノード名・CTRL/BEAT/NOTE・`bpmQ8`「×8整数」表現、
  制御周期5ms・20Hz・約200Hz・同期20ms・通信遅延30ms・BPM 40〜240 が章間で一致。
- 軽微な所見:
  - (a) MOP のフェーズ別散文（§単体テストMOP `.tex:374`、§システムテストMOP `.tex:362`）が、
    MOE 表（`.tex:332-345`）の各 MOE 3指標のうち2指標しか散文化していない。単体テストは
    拍検出・音階解析のみで「楽譜との相違／誤ノート発音数0」が、システムテストは
    パケットロス・CPU負荷のみで「起動時間」が、それぞれ散文に登場しない。表と散文の
    網羅範囲のずれ。意図的な要約か確認したい。
  - (b) R-2（`.tex:459`）「各パートは入り拍をずらして輪唱とする」— ドラム（`node_06`・
    入り拍0拍・リズム伴奏）は輪唱声部ではないため「各パート」がやや不正確。表
    `tab:part-map` で補足はある。重要度 Low。
  - (c) `\cite{ref:douki}`（河瀬2014）を「数十ms程度のずれは聴き手にほぼ気づかれない」の
    根拠に使用（`.tex:371`）。progress.md によれば出典を Barbosa→河瀬2014 へ差し替えた
    経緯がある。河瀬2014（合奏のタイミング調整に関する心理学評論）が「数十ms気づかれない」
    という記述を実際に含むかは原典で要確認（本環境では原典未参照＝要調査）。

**朝の人間レビューで確認したい質問**:
1. production を楽器5台（`node_02〜06`）とする決定は確定か。確定なら `OrcProtocol.h:53`・
   `docs/architecture/protocol.md:136` を5台へ追従させ、`firmware/production/node_06` を
   足すか「4台雛形どまり」と明記するかを決めたい。
2. MOP のフェーズ別散文が MOE 表の一部指標のみを扱うのは意図的な要約か（所見 a）。
3. 提出前に最新 `.tex` で再ビルドし PDF を更新済みか（本環境では再ビルド未実施）。

### トピック2: 夜間レビュー指摘の取り込みとドキュメント乖離の一括修正

**やったこと**: 2026-05-15〜19 に夜間レビュー（2026-05-15/17/18）を cherry-pick で取り込み、
docs の楽器台数・音色 JSON パス・SystemData 構造体名・`partId` 範囲などを実コードへ整合
（`d4fa9ee`〜`029fef0`）。コード変更を伴わないドキュメント修正で完了済み。

**完了度**: 当時の対応は完了。ただし本レビューの章1.2（`protocol.md:107` の beatNo
「0オリジン」）・章1.1（`protocol.md:136` の partId「0x02-0x05」）は当時のスイープで
取り残された箇所。**設計判断・品質・コンテンツ品質**: 該当所見なし（doc 整合のみの作業）。

**朝の確認質問**: なし（章1の修正で回収可能）。

---

## 5. 次のアクション

朝の人間レビューで優先的に着手すべき3件。

1. **【人間判断ブロッカー】production の楽器台数を確定する**（章1.1・トピック1 質問1）。
   5台（`node_02〜06`）で確定なら、`OrcProtocol.h:53` コメントと
   `docs/architecture/protocol.md:136` を5台へ追従し、`firmware/production/node_06` の
   要否を決める。台数が未確定だと下流の doc 修正が手戻りするため最優先。
2. **`docs/architecture/protocol.md:107` の beatNo「0オリジン」を「1オリジン」へ修正**
   （章1.2）。`api.md`・結合レポートと矛盾する明確な誤りで、台数判断に依存せず単独で
   修正可能。
3. **`AGENTS.md:78` の「CI が全ノードをビルド」を実態に合わせて修正**（章1.3）。
   「test_v1・test_v2 の全ノード（production は対象外）」へ文言を改める。これも単独で
   修正可能。

人間判断が必要なブロッカーは #1（production 台数の確定）のみ。#2・#3 は即修正可能。
