# 夜間レビューレポート 2026-05-23

過去 7 日間（2026-05-17 以降）の差分を主対象に、ドキュメント整合・矛盾・改善余地・
直近トピックの深掘りを実施。今週の主活動は **結合レポート全面リライト Phase 4〜6
（提出前最終チェック含む）** と **ADR-0004 楽器 5 台化** で、ファーム実コードへの
変更は無し。

---

## 1. ドキュメント同期チェック

### 1.1 production の楽器台数: ADR-0004 改訂が docs/ ・コード・パケット定義に伝搬していない（**最大の指摘**）

2026-05-21〜22 の一連の作業で「production 想定 = 楽器 5 台（金管 4 ＋ ドラム、partId
`0x02〜0x06`）」へ統一する方向に動き、以下まで反映済み:

- `AGENTS.md:8` 「production（本番想定）は 5 台（node_02〜06、金管 4＋ドラム）」
- `.agent/architecture.md:7` 「production: 楽器 5（node_02〜06）の 6 台構成」
- `.agent/api.md:50` 「production 想定は 0x02〜0x06（楽器 5 台＝金管 4 + ドラム）」
- `docs/src/content/docs/decisions/0004-ensemble-structure.md:25` 「楽器 × 5（金管 4 ＋ ドラム 1）」
- `report/計画書_中間発表/23_計画書・設計書.tex:458-459` R-1 6 台同期演奏・R-2 主旋律 4 ＋ ドラム 1
- `report/計画書_中間発表/23_計画書・設計書.tex:526-530` node_02〜06 の part 対応表

ところが **以下は依然「production 想定 = 楽器 4 台（node_02〜05、partId 0x02〜0x05）」のまま** で、
新方針と矛盾している:

| ファイル | 行 | 現状の記述 | 期待 |
|---|---|---|---|
| `firmware/test_v2/common/lib/OrcProtocol/OrcProtocol.h` | 53 | `test_v2 は 0x02-0x04 / production 想定は 0x02-0x05` | `production 想定は 0x02-0x06` |
| `pc_app/test_v2/orchestra_resynth/orchestra_resynth.pde` | 39 | 同上 | 同上 |
| `pc_app/test_v2/orchestra_resynth/orchestra_resynth.pde` | 391 | `for (int p=0x02; p<=0x05; p++)` UI 表示ループ | `p<=0x06` に拡張（または test_v2 の 0x04 のままに戻す） |
| `pc_app/test_v2/README.md` | 56 | `production 想定は 0x02–0x05` | `0x02–0x06` |
| `firmware/README.md` | 84 | `node_05 ドラム（未実装）` 1 行のみ | node_05/06 の構成を 5 楽器版へ更新 |
| `docs/src/content/docs/architecture/protocol.md` | 136 | `production 想定は 0x02〜0x05（楽器 4 台）` | `0x02〜0x06（楽器 5 台）` |
| `docs/src/content/docs/firmware/orc-net.md` | 42 | `production 想定は node_02-05` | `node_02-06` |
| `docs/src/content/docs/firmware/orc-protocol.md` | 204 | `production 想定は 0x02-0x05` | `0x02-0x06` |
| `docs/src/content/docs/intro/glossary.md` | 44-45 | `production 想定は 4 声（node_02〜05）` | `5 声（node_02〜06）` |
| `docs/src/content/docs/intro/glossary.md` | 90 | `production 想定では 0x02〜0x05` | `0x02〜0x06` |
| `docs/src/content/docs/intro/overview.md` | 27, 31, 41, 112 | `production 想定は 5 台/楽器 4 台/5 番目のノード（node_05）` | 「production = 楽器 5 台、合計 6 台」へ更新 |
| `docs/src/content/docs/essentials/project.md` | 29, 70, 98 | `production 想定は 4 台` | `5 台（node_02〜06）` |
| `docs/src/content/docs/team/roles.md` | 30-31, 51 | 楽器 4（node_02〜05）、`node_05 = パート D`、コーディング担当行は 4 行 | node_06 を追加して 5 行に拡張、または「ADR-0004 改訂で 5 楽器化された旨」の脚注 |
| `docs/src/content/docs/decisions/0005-firmware-embedded-module-architecture.md` | 15 | `Arduino UNO R4 WiFi × 5 台（指揮者 1 ＋ 楽器 4）` | ADR-0004 改訂と整合させ「指揮者 1（XIAO ESP32-S3）＋ 楽器 5（UNO R4 WiFi）」へ |
| `docs/src/content/docs/decisions/0007-project-purpose-and-scope.md` | 86 | `4 声の役割分担` 未決 | `5 声` へ |

**重要度 High**: ADR-0004 改訂の直接整合。コード・README・主要 docs の併存で、
新規読者は「結局何台か」を判断できない（5 台/6 台/楽器 4/楽器 5 が同居）。

**修正案**: 一括 grep 置換で対応可（`0x05` を直接置換すると test_v2 の `0x02-0x04`
記述を壊さないかだけ注意。原則 `partId 範囲が 0x02-0x05` の文字列を `0x02-0x06`、
`楽器 4 台` を `楽器 5 台` に。`pc_app/.pde` の `p<=0x05` ループは production 反映
のタイミングを揃えてからにすべきで、コード本体は実機テスト要のため別途扱い）。

### 1.2 楽器ノード台数を「3 台」と表記している docs（test_v2/production の混同）

ADR-0004 とは別レイヤの 1 台ズレ。test_v2 実体は **3 台（node_02〜04）** だが、
docs/intro/overview.md などで「test_v2 は 4 台」と書かれている箇所が混在。

| ファイル | 行 | 現状 | 期待 |
|---|---|---|---|
| `docs/src/content/docs/intro/overview.md` | 27 | `現行 test_v2 は 4 台` | `test_v2 は 4 台`（指揮者 1 ＋ 楽器 3 = 全体 4 台。「楽器 4 台」と読み違える表現） |
| `docs/src/content/docs/intro/overview.md` | 41, 112 | `5 台が production 想定`、`node_01〜04 の 4 台で稼働中` | `production 想定は楽器 5 台＝全体 6 台` に修正 |

実体（指揮者 + 楽器 N で「全体何台」か）vs（楽器のみ何台か）の表現揺れが原因。
報告書本体（.tex）は「指揮者 1 ＋ 楽器 5 = 全体 6 台」と明示し統一済み。docs も
同じパターン（楽器 N 台、全体 N+1 台）に書き換えると曖昧さが消える。

**重要度 Med**。1.1 と一括で直すと効率良い。

### 1.3 `docs/src/content/docs/architecture/ema.md:153` の架空モジュール名

```
| node_02〜04（楽器） | `ScoreModule`、`NoteEmitterModule`、`OrcReceiverModule` |
```

`ScoreModule` と `NoteEmitterModule` はリポジトリに存在しない（実体は
`NoteSenderModule` + `score_data.cpp` データファイル）。2026-05-15〜18 の夜間
レビューで他箇所の架空名は一括修正された (`ReceiverLogicData` 等) が、ここだけ
取りこぼし。

**修正案**: `OrcReceiverModule、NoteSenderModule（および src/score_data.cpp のデータ）`
へ。重要度 Low（記述ミス、実害はない）。

### 1.4 `docs/src/content/docs/code/firmware.md:235` の楽譜進行式に -1 補正欠落

```
ロジック側（applyPattern.cpp）は `beatNo - headRestBeats` から楽譜位置を計算する。
```

実コード (`firmware/test_v2/node_02/src/applyPattern.cpp:158-159`) は
`firedBeatNo - 1 - headRestBeats`。beatNo が 1 オリジンなので -1 が必要。
報告書 .tex では中⑫指摘で修正済み（行 975-977）だが docs は未追従。

**修正案**: `firedBeatNo - 1 - headRestBeats`（または「`beatNo` は 1 オリジンなので
0 オリジン化して `-1 - headRestBeats`」）。重要度 Low。

### 1.5 `firmware/production/node_01/platformio.ini` の board 設定（3 夜連続保留）

```
[env:uno_r4_wifi]
platform = renesas-ra
board = uno_r4_wifi
```

production の node_01 は指揮者で、現行 test_v2 と同じ運用なら XIAO ESP32-S3 の
はず（ADR-0003）。`firmware/test_v2/node_01/platformio.ini` は
`platform = espressif32@6.10.0 / board = seeed_xiao_esp32s3` を採用済。

`firmware/production/README.md` の冒頭が「PlatformIO の新規プロジェクトを作った
直後と同じクリーンな状態」と明言しているので **意図的に雛形のまま** という解釈も
できる。3 夜連続保留中なので、本レビューでは「保留継続」または「README に
『指揮者は XIAO ESP32-S3 に書き換えること』の一文を足す」のいずれかを選ぶ判断を
仰ぐ。重要度 Med（実機ビルド検証要、コード本体不変）。

### 1.6 `.agent/api.md:221` JSON 例の `name` が実体と不一致

```json
{
  "name": "金管 1",
  ...
}
```

実体（`pc_app/test_v2/orchestra_resynth/data/`）の各 `name` フィールド:
- `0_organ.json` → `example_organ`
- `1_flute.json` → `フルート３`
- `2_bell.json` → `synth_bell`
- `3_flute_tweaked.json` → `フルート３ (調整)`

例として「金管 1」を出すと未来想定（ADR-0004 production の金管 4 パート）と
読まれるが、test_v2 の data/ には存在しない。例示なので大きな問題ではないが、
api.md は SSOT を標榜しており、実体を例にした方が誤読を減らせる。

**修正案**: 例を `"name": "example_organ"` 等の実体に置き換える、または
「（例。実体は `0_organ.json` などを参照）」のコメントを添える。重要度 Low。

### 1.7 `firmware/README.md:81-87` 役割表が test_v2 の 3 台輪唱を反映していない

```
| node_02 | 楽器 1（金管 1 / C4 ベース）| 輪唱 声部 1（headRest=0, instr=0） |
...
| node_05 | ドラム（未実装）| — | (未実装) | 0x05 |
```

行 84 `node_05 ドラム（未実装）` は ADR-0004 改訂前の構成（楽器 4 台、ドラムが 4 台目）。
ADR-0004 改訂後は node_05 は金管 4・node_06 がドラムになる。`firmware/test_v2/`
配下に node_05 のフォルダは無いので test_v2 列を `—` のまま、production 列を追加
する案がきれい。

**重要度 Med**。1.1 と一括処理推奨。

### 1.8 `docs/src/content/docs/firmware/main-instrument.md:501` で `durationQ8` を「Q8 固定小数」と書いている

```
`durationQ8` は 256 = 1 拍 の Q8 固定小数。
```

`durationQ8` は実際 **256 倍** なので「Q8 固定小数（×256）」表現で正しい
（`bpmQ8` だけが ×8 で例外。2026-05-17 修正済）。ただし他の docs（
`docs/.../firmware/orc-protocol.md`）が「`bpmQ8` の `Q8` は例外で ×8、
`durationQ8` 等は本来の Q8 = ×256」と分けて説明している中、
main-instrument.md と `deep-dive/score-progression.md:64` は単に「Q8 固定小数」と
書くだけで、`bpmQ8` の例外を読者に思い出させる注釈が無い。

**修正案**: 「`durationQ8` は本来の Q8 固定小数（256 = 1 拍）。`bpmQ8` だけは命名の歴史的経緯で ×8 整数」の一文を追加。重要度 Low（記述自体は正しい）。

### 1.9 ADR-0007 が ADR-0004 改訂を反映していない

`docs/src/content/docs/decisions/0007-project-purpose-and-scope.md:86` 「4 声の
役割分担」「ゲームモード等の拡張要素の採否」が未決として列挙されたまま。

- 5 声化（ADR-0004 改訂）
- ゲームモード採用（報告書 Phase 4A-4D で R-5/R-6・F7 として正式化）

の 2 件は既に決着しているので、ADR-0007 を「→ ADR-0004 改訂（2026-05-21）で
解決」「→ 報告書 Ch2 R-5/R-6 で採用決定」の参照に書き換えるか、未決リストから
削除する。重要度 Low（読者を旧情報に誘導するレベル）。

---

## 2. バグ・矛盾レビュー

### 2.1 `pc_app/.pde:391` の声部表示ループが ADR-0004 改訂で時限爆弾化

```
for (int p=0x02; p<=0x05; p++){
  String ev = lastEventByPart[p];
  ...
}
```

test_v2 は 0x02〜0x04 で十分。production 想定が 0x02〜0x06 に変わったため、
production 移行のタイミングで `0x06` の声部が UI に出ない。`lastEventByPart` は
`new String[256]` なので配列外参照は無いが、表示で抜ける。

**重要度 Low（test_v2 では実害なし、production 移行時に気づく）**

**修正案**: `for (int p=0x02; p<=0x06; p++)` または `int MAX_PART = 0x06;` 定数化。
1.1 の一括対応時に併せて。実機テストが要るので CLAUDE.md ルールに従い保留候補。

### 2.2 楽器ノード 3 台の `NoteSenderModule` / `OrcReceiverModule` 重複（過去レビュー継続指摘）

`firmware/test_v2/node_02/lib/NoteSenderModule/` `OrcReceiverModule/` が
node_03/04 にもコピーされている（ヘッダコメントと `[N2]/[N3]/[N4]` ログタグ 1 行
を除いて完全一致）。`firmware/test_v2/common/lib/` に移し、ログタグはノード ID で
動的に切り替えるのが筋。

production の 5 台化が現実味を帯びるとコピーが 2 倍に増える（node_02〜06 で
5 セット）ので、production 着手前に集約しておくと工数が圧縮される。

**重要度 Med**（コピーミスのリスク。2026-05-15 のレビューで sed 過誤を実際に
踏んでいる事例あり）。実機テストが要るので保留扱いだが、production 着手と同時の
タイミングが好機。

### 2.3 `int32_t` wraparound（既知保留）

`firmware/test_v2/node_02/lib/OrcReceiverModule/OrcReceiverModule.cpp:13` で
`int32_t sample = (int32_t)(timestampMs - millis())` は、両者 `uint32_t` の差を
取って `int32_t` キャストする実装。`millis()` が 49.7 日でラップするため、
理論上は long-running で問題化しうる。実演 5 分なら無視可能。

**重要度 Low（実害なし）**。コメント 1 行（「短期演奏想定。長時間動作なら符号付き差で対応」）の追記で十分。

### 2.4 `production` ディレクトリが CI に含まれていない

`.github/workflows/pio-build.yml` の matrix に `test_v1/*` `test_v2/*` のみ。
production の node_01 が ADR-0003 と整合する `board = uno_r4_wifi` のまま
（指摘 1.5）になっている根本原因。

production を CI に加えても **現状の `board = uno_r4_wifi` でビルドは通る**（雛形
スケッチが最小限のため）。1.5 を実装した瞬間に CI が落ちる仕組みを敷くなら、
1.5 の修正と同時に matrix へ追加するのが安全。

**重要度 Low（保留継続）**。production が雛形のうちは無理に追加する必要なし。

### 2.5 報告書 PDF `report/計画書_中間発表/23_計画書・設計書.pdf` のフォント警告

`.agent/activeContext.md` 既知の論点に「undefined 3 件は Hiragino W6 bold の
Font Warning で実害なし」とあるとおり、LuaLaTeX で Hiragino W6 bold series を
要求する箇所が `undefined` 警告を出す。提出には影響しないが、長期的には
`hack-fonts.sty` の bold セット定義に W6 fallback を入れると消える。

**重要度 Low**。提出後の改善で十分。

### 2.6 「`pc_app/test_v2/orchestra_resynth/data/`」と「`sound_lab/`」の運用が
docs と report で微妙にズレている

api.md (`/.agent/api.md:234-235`) は「`sound_lab/` 配下は試作の実験場、完成 JSON は
`pc_app/.../data/` にコピー」と明記。報告書本体 .tex は `pc_app/.../data/` のみ
扱い `sound_lab/` 不在。docs (`pc-audio/instr-model.md` 等) は引き続き
`sound_lab/` を分析パイプラインの主舞台として扱う。

実態に合致しているのは「Processing が読むのは `pc_app/.../data/`」「`sound_lab/`
は分析・試作のみで Processing は読まない」だが、`sound_lab/data/` も配置上は
存在する（`sound_lab/data/*.json` が試作用に残っている可能性、要調査）。

**重要度 Low（読者の混乱要因）**。説明を api.md 表現で統一すれば解決。

---

## 3. 改善・新機能アイデア

### 3.1 ドキュメント間「楽器台数 SSOT」の一元化スクリプト
**なぜ**: 指摘 1.1 のような台数表記の食い違いが過去 1 か月で繰り返し起きている
（3 台/4 台/5 台が混在）。AGENTS.md または api.md に「production = 6 台（指揮者 1
＋ 楽器 5、partId 0x02-0x06）」と宣言する 1 か所だけを SSOT として、CI で
他ファイルに対する表記揺れ（`楽器 4 台` `node_02〜05` `0x02-0x05` の出現）を
検知するスクリプトを足すと、ADR 改訂時の伝搬漏れを構造的に防げる。
**工数**: S（shell script + GitHub Actions 30 行程度）。
**優先度**: Med（一度仕組み化すれば再発しない）。

### 3.2 楽器ノードの `common/lib/` 集約（指摘 2.2 の根治）
**なぜ**: 2026-05-19 の sed 過誤で実際にバグを踏んだ。重複ファイルがある限り
類似のミスは再発する。集約すれば 1 箇所修正で全ノード反映できる。
**工数**: S（ファイル移動 + 各 node の `platformio.ini` の `-I` パス更新）。
**優先度**: Med-High（production 5 台化の準備）。実機ビルド検証要。

### 3.3 `firmware/test_v2/common/lib/OrcProtocol/OrcProtocol.h:38` `CtrlPayload.state`
コメントを将来構想の ModeSelect まで含めて明示
**なぜ**: 報告書本体 .tex は `0=Idle, 1=Calibrating, 2=Conducting, 3=Fallback, 4=ModeSelect` と
記述する（行 926。Phase 4A でゲームモード設計の一部として追加）一方、OrcProtocol.h は
`0=Idle 1=Calibrating 2=Conducting 3=Fallback` のみ。コードと報告書のいずれも
それぞれで筋は通っているが、コメントに「将来 4=ModeSelect を予約」と一行足すと
ファームと設計書の橋渡しになる。
**工数**: S（コメント 1 行追加。実機ビルド不要なほど軽い変更）。
**優先度**: Low。

### 3.4 報告書 .tex の整合性 CI（latexmk + Overfull カウンタ）
**なぜ**: 編集のたび「`latexmk -lualatex` を手で回して Overfull/Underfull 0
であること」を毎回ユーザー側で確認する運用。GitHub Actions に Docker image
（`paperist/texlive-ja` または LuaLaTeX 環境）を載せ、ビルド失敗・Overfull 発生・
未定義参照を CI で検出できる。
**工数**: M（Docker 環境構築・キャッシュ・Lua 用は別途 + Hiragino フォント代替）。
**優先度**: Low（提出済みなので緊急性無し。卒業発表まで継続更新するなら採算合う）。

### 3.5 docs サイトの「version badges」
**なぜ**: 各ページが test_v1/test_v2/production のどれを前提に書かれているかが
読者に伝わらない（指摘 1.2 の根本原因の一つ）。Starlight の frontmatter に
`version: test_v2` を入れ、ページ冒頭にバッジ表示するコンポーネントを足すと、
迷子になりにくい。
**工数**: M（Starlight カスタムコンポーネント + 全ページに frontmatter 追加）。
**優先度**: Low。

### 3.6 `firmware/test_v2/common/lib/SerialDebug` のドキュメント化
**なぜ**: 楽器ノードが `SERIAL_DEBUG=0` のときバイナリ NOTE パケットを流し、
`=1` のときテキストログを流す切替ポイントは PC アプリ運用に直結するが、
SerialDebug の README が薄い（`platformio.ini` の `-DSERIAL_DEBUG=0` コメント程度）。
**工数**: S。**優先度**: Low。

---

## 4. 直近作業のディープレビュー（過去 7 日）

過去 7 日のコミット（45 件）は実質 2 トピックに収束する。

### トピック A: 結合レポート全面リライト Phase 3〜6（最終チェック含む）

#### やったこと（時系列）

- **2026-05-19**: Umezawa/Saito が個人版計画書 TeX 2 本を投入（`24a1a0/9597af`）。
- **2026-05-20**: Kjibiki が結合済み TeX（3077 行・178 KB）を `計画書結合/` へ投入
  （`d44171`）。Claude が「肥大の主因は Ch2/Ch3 の 2 版収録」と分析し、
  `_作業計画.md` で Phase 0〜4 を策定（`36f4f4`）。Phase 0（環境構築・骨格、
  `a82fa88`）と Phase 1（Ch1 計画書、11 ページ・`6430c27`）を実施。
- **2026-05-21**: Phase 2（Ch2 基本設計、`d387073`）・楽器 4→5 台化追従
  （`d013dec/7c67b71`）・Phase 3（Ch3 詳細設計、`b9ec336`）・ゲームモード追加で
  計画 Phase 4A〜4D 再分割（`de6b1c4`）・Phase 4A〜4D（ゲーム反映・TikZ 全廃・
  シーケンス図化、`3e15fdc / 23857f8 / 010643d / 56a2833`）。
- **2026-05-21**: Phase 5（状態遷移の動作シーケンス図化、`5c40eef`）・ガント整合
  修正（`4f997f1`）・最終チェックで指摘 15 件起票（`eda3870`）・即修正 5 件
  （`b678370 / 53fbf7c`）・Noname00356 が句点「。→．」一括統一（`19edb91`）。
- **2026-05-22**: 残 10 件のうち 8 件処理（`eda39d7`）、ADR-0004 を 5 台化改訂
  （`348a608`）、PDF 再生成（`7381948`）、最終 2 件（高③・中⑨）で全 15 件決着
  （`f3519c7 / 23c2d72`）。

最終成果: 本体 53 ページ・Overfull/Underfull 0・未定義参照 0。報告書側の
事実関係（楽器 5 台 / ゲームモード / 課題曲かえるのうた）は提出版として確定。

#### 完了度

ユーザー側の「最終チェック 15 件決着」が完了宣言で、提出可能な状態に到達済み。
ただし以下の **報告書外への伝搬** が未完:

- 指摘 1.1（partId 範囲 / 楽器台数）の `OrcProtocol.h` / `.pde` / 各種 docs。
  報告書本体は新方針で揃ったが、関連リポジトリ全体は旧方針が残り続けている。
- ADR-0007 の「未決事項」記述（指摘 1.9）。Phase 4A〜4D でゲームモードを採用と
  決めた一方、ADR-0007 はゲームモードを未決のまま。
- docs/architecture/ema.md:153 の架空モジュール名（指摘 1.3）。
- `team/roles.md:51` の 4 台担当表（指摘 1.1 の派生）。

#### 設計判断

「2 モード（自由演奏 + ゲーム）構成」「メトロノームガイドのフェードアウト」
「司令塔 = node_01」「CTRL 予約 4B を mode + targetBpmQ8 + score に割当」は、
パケット長を変えずに既存プロトコルへゲームモードを追加する判断として妥当。

CTRL の `state` 値再採番（ModeSelect を 4 へ後置で既存値を保つ）も互換性
重視で良い選択。ただし `OrcProtocol.h` のコメントが旧 4 値のままなので、
コメントだけでも追従しておくとファームと設計書が突き合わせやすい（改善 3.3）。

#### 品質（コード系）

- `latexmk -lualatex` ビルドはユーザー側で都度確認し成功（53 ページ・Overfull/
  Underfull 0・未定義参照 0）。
- 図 8 点が draw.io 化されており TikZ 全廃。`.drawio` ソースと `.png` 書き出しが
  両方コミットされ後編集可能。
- 句点を「．」に統一（19edb91）。本体 .tex のみ対象で `.agent/` `.md` は据え置きの
  運用ルールが activeContext に記録済み。

懸念: 報告書本体 .tex 編集と同時に PDF を再ビルド・コミットする運用が確立して
いるため、コミット数は多いがリンク・参照の食い違いは起きていない。

#### コンテンツ品質（報告書）

論理破綻・引用ズレは最終チェック 15 件で出尽くしており、決着済み:

- 高①: ref:douki を Barbosa→河瀬 2014 へ差し替え（本文「数十 ms 気づかれない」と
  整合する出典）。
- 高②: NOTE パケット表に instrumentId 行追加。
- 高③: 「平均 5.52 cent」を ref:onkai の要旨記載「3.6 cent 未満」へ修正。
- 中④: CTRL state を 0=Idle/1=Cal/2=Cond/3=Fallback/4=ModeSelect で再採番。
- 中⑥: 「振り速度→音量」を「振りの大きさ→強弱」へ統一。
- 中⑨: MOE 音階誤差の測定方法を「合成出力音録音 → 周波数分析 → 平均律理論音高
  との差」と明記。

引用の正確性: 参考文献 6 件すべて被引用（teru/ninntenndo/ref:douki/ref:chien/
ref:beat/ref:onkai）。

章立ての一貫性: Ch1 計画 / Ch2 基本 / Ch3 詳細 / Ch4 生成 AI / 参考文献の 4 章
+ 文献で、テンプレート `plan_template.tex` の章立てを踏襲。Ch2 と Ch3 の重複
（旧版 Block A/B 併存）も Phase 2-3 で解消済み。

#### 朝の人間レビューで確認したい質問

1. **指摘 1.1 の一括修正の優先度**: 報告書本体は提出済として、`OrcProtocol.h` の
   コメントや docs/intro/glossary.md など旧方針を残しているファイル群を「いつ」
   一括更新するか? 現時点で混在しているため、新規読者やレビュアーが混乱する。
2. **コードへの伝搬（指摘 2.1 の `.pde` ループ範囲）**: production 移行と
   セットの修正にするか、それとも先行して直すか?
3. **ADR-0007 の更新**: ゲームモード採用と 5 声化を ADR-0007 から削るのが
   筋（決定済として ADR-0004 改訂・報告書 R-5/R-6 を参照）。実施してよいか?

### トピック B: ADR-0004 楽器 5 台化（金管 4＋ドラム）

#### やったこと（時系列）

- **2026-05-21**: 報告書 Phase 2 でユーザー決定（楽器 4→5 台）。
  `d013dec` で AGENTS.md と `.agent/architecture.md` を 5 台化、`7c67b71` で
  api.md の partId 範囲を `0x02-0x06` 化。
- **2026-05-22**: `348a608` で ADR-0004 を改訂（改訂履歴を本文に明記）。

#### 完了度

ADR-0004 本体・AGENTS.md・.agent/architecture.md・.agent/api.md・報告書本体
までは 5 台化が完了。残りの伝搬作業は **指摘 1.1 の一覧** がそのまま残課題。

#### 設計判断

「指揮者 XIAO ESP32-S3 を別枠にし、楽器用 UNO R4 WiFi を 5 台で授業制約『人数分
－ 1 ＝ 5 台』を満たす」整理は ADR-0004 改訂で明示されており、論理が通っている。

#### 品質（コード系）

ADR-0004 改訂は decision のみで `firmware/` 配下を変更していない。production
ディレクトリの実装着手は未だ無し。CI に production を含めない状態は維持
（指摘 2.4）。

#### コンテンツ品質（ADR）

ADR-0004 改訂版（行 52-55）は改訂履歴を箇条書きで明示しており、設計判断の経緯が
追える。「初版（2026-04-15・楽器 4 台）の決定日・理由は残し、タイトル・決定事項・
改訂履歴を更新」というポリシーが守られている。

#### 朝の人間レビューで確認したい質問

1. **docs/decisions/0005-firmware-embedded-module-architecture.md:15** が
   「Arduino UNO R4 WiFi × 5 台（指揮者 1 ＋ 楽器 4）」のまま。ADR-0005 を
   ADR-0004 の改訂と整合させる修正をかけるか?（ADR-0005 自体の主張は不変、
   前提となる台数表記のみ更新）。
2. **`firmware/production/` の着手タイミング**: 5 台化が確定したが、production
   側は依然 4 ノード（node_05 まで）。node_06 ディレクトリを追加するのか、
   そもそも test_v2 の 3 台輪唱を完成度高く育てる方針か?

---

## 5. 次のアクション

朝の人間レビューで最優先で着手すべき 3 件:

### アクション 1 (Med-High): 指摘 1.1 の一括ドキュメント更新

「production = 楽器 5 台（node_02〜06）、partId `0x02-0x06`」へ統一する 14 箇所
（指摘 1.1 表 + 1.2/1.7）の一括 grep 置換。3 夜連続で同じテーマが指摘されており、
報告書本体の整合が取れた今こそ全リポジトリへ伝搬させる絶好のタイミング。

実機テスト不要のドキュメント変更のみ（`.pde:391` UI ループや `OrcProtocol.h:53`
コメントもコード本体ではないので含めて可、ただし `.pde` ループ変更は実機で
表示確認推奨のため後回し可）。Claude が自走可能。

### アクション 2 (Med): docs/architecture/ema.md:153・docs/code/firmware.md:235・ADR-0005/0007 の取りこぼし整合

- ema.md:153 の架空モジュール名（指摘 1.3）
- code/firmware.md:235 の楽譜進行式の -1 補正欠落（指摘 1.4）
- ADR-0005 line 15 の「指揮者 1 ＋ 楽器 4」を ADR-0004 改訂と整合（トピック B Q1）
- ADR-0007 の未決リストからゲームモード・5 声を削除（指摘 1.9）

実機テスト不要、Claude 自走可能。アクション 1 と同じセッションでまとめて処理可。

### アクション 3 (Med、人間判断必要): 楽器ノード重複ライブラリの `common/lib/` 集約計画決定

指摘 2.2。production 5 台化への準備として、`NoteSenderModule` /
`OrcReceiverModule` の `firmware/test_v2/common/lib/` 移動を計画。実機テストが
要るため、ユーザーが「いつ・誰が・実機検証込みで」やるかを決める必要あり。
2026-05-19 の sed 過誤の再発防止策。

---

### 人間判断が必要なブロッカー

- **指摘 1.5 (production node_01 の board)**: 3 夜連続保留。XIAO ESP32-S3 化
  するか、README で「指揮者役なら書き換え必要」と注記するかの判断。
- **指摘 2.4 (production を CI 対象に追加)**: 1.5 とセット。production が
  雛形扱いの間は据え置きで OK だが、本格着手と同時に追加する判断が要る。
- **トピック B Q2 (production 着手タイミング)**: 中間発表は終わり、発表会
  2026-07-01 に向けて test_v2 を磨くか production に手を広げるかの方針判断。
