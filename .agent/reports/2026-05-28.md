# 夜間レビュー 2026-05-28

直近 7 日（2026-05-21〜2026-05-28）の作業を対象に、ドキュメント同期・バグ／矛盾・改善案・直近作業ディープレビューを実施。
レビュー時点の HEAD は origin/main `e170ba7 [修正] 夜間レビュー累積指摘の追従 (2026-05-28) (#18)`。
本ブランチ `claude/nightly-report-2026-05-28` は origin/main 起点で、未取り込みコミットはなし。

PR #18 (`e170ba7`) で 2026-05-23〜27 の連続指摘（ADR-0004 5 台横展開・`beatRedundancy=4`/`beatGapMs` の暫定切り分け記載・楽譜進行式 -1 補正・架空モジュール名）の大半が解消したため、本レビューは「5/28 取り込み後の残存」を主軸に整理する。

---

## 1. ドキュメント同期チェック

### 1.1 楽器ノード node_02 / node_03 の `SERIAL_DEBUG=1` が node_04 と非対称【重要度: High】

5/27 のレビュー指摘 1.4 と同じ問題が `e170ba7` 取り込み後も未解消で残っている（PR #18 は docs / コメントのみで `.ini` には触っていない）。

- `firmware/test_v2/node_02/platformio.ini:33` `-DSERIAL_DEBUG=1`
- `firmware/test_v2/node_03/platformio.ini:33` `-DSERIAL_DEBUG=1`
- `firmware/test_v2/node_04/platformio.ini:33` `-DSERIAL_DEBUG=0`

`.agent/conventions.md` ロギング節「楽器ノード（node_02〜04）はデフォルト `SERIAL_DEBUG=0`」と矛盾。
`SERIAL_DEBUG=1` 状態だと `NoteSenderModule` は 20B バイナリ NOTE パケットの代わりに人間可読テキストを `Serial.print` するため、`pc_app/test_v2/orchestra_resynth/orchestra_resynth.pde` と `work/umezawa/hck/processing/processing.pde` のいずれから見ても node_02 / node_03 だけ無音になる。コミット `ac8c5ff デバック`（2026-05-25 22:44）以降そのままで、`e170ba7` のコミットメッセージ「コメントとドキュメントのみの変更。ファーム本体ロジックは不変」が示すとおり、本件は意図的に保留された可能性が高い。

修正案: 演奏したいときは全 node を `SERIAL_DEBUG=0` に揃える。デバッグ継続中なら `progress.md` に「`ac8c5ff` の SERIAL_DEBUG=1 は切り分け継続中で、本番演奏前に 0 へ戻す前提」と 1 行記録。最終的に恒久値を決めて `progress.md` に意図を残すまで「次の夜間レビューでも同じ指摘が再浮上する」状態が続く。

### 1.2 `architecture.md:6` の「きらきら星 3 声輪唱で稼働中」と `activeContext.md:54` の「課題曲『かえるのうた』はチーム判断で確定」の用語不一致【重要度: Med】

5/27 指摘 1.5 と同じだが、`e170ba7` は本件を扱っていない。

- `.agent/architecture.md:6` 「きらきら星 3 声輪唱で稼働中」
- `.agent/architecture.md:86` 表「きらきら星の輪唱（3 声部）」
- `.agent/architecture.md:161` 「きらきら星では全行 0」
- `docs/src/content/docs/architecture/three-stages.md:23,42` 「きらきら星 3 声輪唱」「曲: 『きらきら星』3 声輪唱」
- `docs/src/content/docs/intro/overview.md:98` 「『きらきら星』3 声輪唱が動作」
- 一方 `.agent/activeContext.md:54` 「課題曲『かえるのうた』はチーム判断で確定（中⑦）」
- `firmware/test_v2/node_0{2,3,4}/src/score_data.cpp` の実体は依然「きらきら星」

修正案: 「test_v2 のテスト題材＝きらきら星（実装済み）」と「production の本番課題曲＝かえるのうた（未実装）」を `architecture.md` 冒頭に書き分ける一文を追加し、`activeContext.md` で「本番課題曲決定済み・楽譜データ作成は未着手」と用途を分離する。曲名差し替えの実コード変更は production 移行時にまとめれば良い。

### 1.3 docs 内「4 声」「2 連送」表記の取りこぼし【重要度: Med】

`e170ba7` は ADR-0004 関連（5 台 / 0x02-0x06）と `beatRedundancy=4` / `beatGapMs` の主要箇所のみを横展開しており、補助節・古い説明文の追従が漏れている。

「4 声」関連（ADR-0004 改訂後は 5 声が正）:
- `docs/src/content/docs/architecture/score.md:121` 「`headRestBeats` を曲構造に合わせて調整（4 声なら 0 / 8 / 16 / 24 など）」
- `docs/src/content/docs/architecture/three-stages.md:64` 「test_v2 で 4 声化・強弱・ビブラート等が安定したら production に取り込む」
- `docs/src/content/docs/decisions/0007-project-purpose-and-scope.md:86` 「4 声の役割分担（どのノードがどの声部か）」
- `docs/src/content/docs/deep-dive/score-progression.md:218,221` 「4 声以上に増やすには」「0 / 8 / 16 / 24 で 4 声がきれいに重なる」（拡張ガイドの一例として 4 声例も残してよい）

「2 連送」関連（実値は `beatRedundancy=4`、暫定値ながら本数記述が誤）:
- `docs/src/content/docs/essentials/project.md:62` Mermaid 内「CTRL 20Hz/<br/>BEAT 2連送」
- `docs/src/content/docs/essentials/project.md:79` 表「BEAT | 指揮者 → 楽器（WiFi、拍ごとに 2 連送）」
- `docs/src/content/docs/code/troubleshooting.md:105` 「原因: BEAT 2 連送の重複検知が効いていない」
- `docs/src/content/docs/firmware/orc-net.md:286` 「BEAT の冗長送信（2 連送）が連続到達した場合」
- `docs/src/content/docs/firmware/orc-net.md:337` 「2 連送なら片方ロスしても通る」
- `docs/src/content/docs/firmware/orc-sender.md:75,115,202,247` 計算例・実装解説で「2 連送」前提
- `docs/src/content/docs/deep-dive/udp-multicast.md:46,48` 「BEAT は **2 連送**」「2 連送が確率的に救う」
- `docs/src/content/docs/deep-dive/score-progression.md:309` 「BEAT の 2 連送が両方届かない（極めて稀）」

修正案: `e170ba7` の追従系コミットメッセージにある「2026-05-25 暫定切り分け中」のスタンスを保つなら、上記箇所も「現状 `beatRedundancy=4`（暫定）。本資料の数式・例示は `=N` の汎用表記または 2/4 併記で書く」に統一すると、確定値を変えるたびに広域置換せずに済む。「4 声」関連は ADR-0004 確定の節と「過去 test_v2 3 声 → 将来 5 声」の歴史を残す節を分けて整理する。

### 1.4 `orc-sender.md` の「2 連送 vs 4 連送」確率計算と本文の矛盾【重要度: Low】

`docs/src/content/docs/firmware/orc-sender.md:74-80` で「`p = 5%` のとき 2 連送なら両方ロス確率 `p² = 0.25%`」を計算した直後（line 79-80）に「2026-05-25 に ESP32-S3 SoftAP で連続ロスが観測されたため `beatRedundancy = 4` に増量」と書いてあり、4 連送時の確率（`p⁴ = 0.000625%`）が記載されていない。読者は「2 連送で十分なはずなのに 4 にしている理由」を推測することになる。

修正案: 「実測値 p_independent ≠ 真の独立ロス率（同一状態連発の癖でロスが時間相関する）」と注記し、4 連送 + `beatGapMs` で時間相関を断つ意図を 1 行で補足する。

### 1.5 `pc-audio/` の「3〜4 声」「3 声」記述が ADR-0004 改訂後の用語に揃っていない【重要度: Low】

`e170ba7` の追従範囲外。

- `docs/src/content/docs/pc-audio/design.md:22` 「同時発音は最低 3 声（輪唱）、目標 3〜4 声 × 複数 PC」
- `docs/src/content/docs/pc-audio/design.md:51` 「倍音数 40 個 × 同時 3〜4 声で 1 Mac は余裕」
- `docs/src/content/docs/pc-audio/signal-flow.md:126` 「ハッカソン規模（3〜4 声輪唱）では到達しない値」

これらは PC 側の「同時発音容量」の説明文脈であり、ADR-0004 改訂後は「最低 3 声（test_v2）、目標 5 声（production・金管 4 + ドラム 1）」が正しい。同時発音数は声部数の整数倍（ADSR の release tail で重なる）になるので、容量見積りの数式自体も見直し対象。

修正案: 「同時発音 5 声 × release tail 重複を考慮し 10〜12 声で見積もる」程度の数値に書き換え。本実装が動くまでは推測値で構わない旨を `pc-audio/design.md` 冒頭で断る。

### 1.6 PR #18 のコミットメッセージで言及されている「ファーム本体ロジックは不変」と `OrcSenderModule.h:16` のコメント変更の取り扱い【重要度: Low】

`e170ba7` 内の commit `[ドキュメント] beatGapMs 追加と beatRedundancy=4 引上げを暫定切り分けとして記載` は、`firmware/.../OrcSenderModule.h:16` の型コメントを `(1-3)` → `(1-8 想定)` に書き換えている（本体ヘッダの変更）。コミットメッセージは「コメントとドキュメントのみの変更」と書いてあり、ロジック変更はないので CLAUDE.md「実機未テスト .ino/.cpp に Claude 起点で追加変更を入れない」ルールには抵触しない判断は妥当。

ただしファームヘッダコメントは厳密には「実機テストの対象になり得る」ため、`progress.md` の 5/28 エントリで「コメント追記のみ・ロジック不変」を明示しておくと、将来「いつ誰が範囲外を触ったか」の追跡がしやすい。本レビュー時点では `progress.md` は 5/25 エントリで止まっている（5/28 の `e170ba7` 取り込みが未記録）。

修正案: `progress.md` の末尾に 5/28 エントリ「PR #18 で 5/23〜27 累積指摘を一括追従」を追記。`activeContext.md` の「次の一手」も e170ba7 後の状態に更新（現状は 5/22 で停止）。

---

## 2. バグ・矛盾レビュー

### 2.1 梅澤 Processing が NotePayload の `instrumentId`（byte 18）を読み飛ばし【重要度: High】

5/27 指摘 2.1 と同じ。`e170ba7` は梅澤 Processing には触れていない。

- `work/umezawa/hck/processing/P03_NoteProtocol.pde:49` `p.durationMs = u16le(frame, 16);` で読み出しが止まっている
- `work/umezawa/hck/processing/P03_NoteProtocol.pde:65-66` `makeNoteFrame()` は byte 18-19 を 0 埋め
- canonical（`pc_app/test_v2/orchestra_resynth/orchestra_resynth.pde:270`）は `int instrumentId = u8(buf[18])` で受け取り `triggerNote(partId, instrumentId, ...)` に渡している

梅澤 Processing では NotePacket の `partId` で固定的に Brass / Rhythm 波形を選ぶだけで、JSON 由来の音色切替（`pc_app/test_v2/orchestra_resynth/data/0_organ.json` 等）が一切できない。指摘 2.2 と組合せて検証すべき。

修正案: `NotePacket` クラスに `int instrumentId` を追加し `frame[18] & 0xff` を読む。`PartManager.handleNote()` / `noteOn()` に instrumentId を引き渡して波形選択を 2 段（partId → 配置, instrumentId → 音色）に分離。`makeNoteFrame()` に instrumentId 引数を追加。

### 2.2 梅澤 Processing は ADR-0004 改訂前の「金管 3 ＋ ドラム 1」前提（5 台未追随）【重要度: Med】

5/27 指摘 2.2 と同じ。

- `work/umezawa/hck/processing/P02_ConstantsAndParts.pde:8-11` `PART_BRASS_1 = 0x02 / PART_BRASS_2 = 0x03 / PART_BRASS_3 = 0x04 / PART_RHYTHM = 0x05`
- `work/umezawa/hck/processing/P05_SoundPartManager.pde:81` `int note = partId == PART_RHYTHM ? 36 : 60 + (partId - PART_BRASS_1) * 4;` も 4 ノード前提
- `work/umezawa/hck/processing/P07_ScreenView.pde:321-322` UI 表示も「金管3 / リズム」止まり
- `work/umezawa/hck/processing/README.md:46-49, 149, 268, 294-295, 301` キー操作表・partId 表・サンプルコードすべて 4 ノード前提
- `work/umezawa/hck/processing/ProgramExplanation.html:966, 1119-1125, 1172-1175` 公開向け説明 HTML も同じ

ADR-0004 改訂後は `0x02〜0x06`（金管 4 ＋ ドラム 1）。`PART_BRASS_4 = 0x05` / `PART_RHYTHM = 0x06` が正しい。

修正案: 梅澤本人作業として ADR-0004 と歩調を合わせ `PART_BRASS_4` 追加、キー `5` を Rhythm に再配置、`P05_SoundPartManager.pde:81` の noteNumber 計算を `60 + (partId - PART_BRASS_1) * 4` のまま 0x06 まで拡張（または partId ごとの個別マッピングに変更）。HTML / README / `isKnownPart()` も同期。

### 2.3 OrcReceiverModule / NoteSenderModule の 3 重複コピーが温存（4 夜連続保留）【重要度: Med-High】

5/15 → 5/18 → 5/19 → 5/27 の 4 夜連続で保留指摘。`firmware/test_v2/node_02/lib/OrcReceiverModule/` / `node_03/lib/OrcReceiverModule/` / `node_04/lib/OrcReceiverModule/` がほぼ同一実装で、ヘッダコメントの「pio run -d 〜」パスのみ差分（実測）。

修正案: `firmware/test_v2/common/lib/` に集約。実機 3 台 + Processing で 1 回演奏確認できれば検証完了。本件は ADR-0004 改訂で「node_05 / node_06 を追加するとコピー先が 3 → 5 個に増える」差分爆発が確実なので、production 雛形整備（指摘 2.5）と前後して片付けると一石二鳥。

### 2.4 production CI 未取り込み（4 夜連続保留）【重要度: Med】

`.github/workflows/pio-build.yml:17-26` の matrix は `test_v1/node_0X` と `test_v2/node_0X` のみ。`firmware/production/node_01〜05` は CI 対象外。

修正案:
- 指摘 2.5 の `production/node_01/platformio.ini` board 修正と組合せ、production matrix を追加。
- ADR-0004 改訂後の `node_06` フォルダがまだ存在しないので、最低限ビルドが通る空 main.cpp + 雛形 `platformio.ini` を作成し CI に乗せる。
- `firmware/production/node_05/` の旧雛形（現行は楽器役）と新 `node_06`（ドラム）の役割割当を最終決定してから着手。

### 2.5 `production/node_01/platformio.ini` の board が `uno_r4_wifi` のまま（4 夜連続保留）【重要度: Med】

`firmware/production/node_01/platformio.ini:14-16` `[env:uno_r4_wifi] platform = renesas-ra` `board = uno_r4_wifi`。指揮者ノードは ADR-0003 で XIAO ESP32-S3 Sense に決定済みなので `seeed_xiao_esp32s3` / `platform = espressif32@6.10.0` が正しい。CLAUDE.md「実機未テスト .ino/.cpp に Claude 起点で追加変更を入れない」ルールから Claude 側は触れない。

修正案: ユーザー手元で 1 回 `pio run -d firmware/production/node_01` を回し、ビルドが通ったら commit。`[env:uno_r4_wifi]` → `[env:seeed_xiao_esp32s3]` に揃える。test_v2/node_01 の `platformio.ini` を参考にコピー可。

### 2.6 梅澤 Processing のヒントテキストが画面状態と矛盾【重要度: Low】

5/27 指摘 2.6 と同じ。`work/umezawa/hck/processing/processing.pde:180` で `refreshSerialPorts()` 内に「`シリアルポートをクリック、または t キーでテスト音`」を出すが、`keyPressed()` の `t` キー処理（`processing.pde:286-287`）は `screenState != SCREEN_PERFORMANCE` で早期 return（line 269）するため、PORT_SELECT 画面で `t` は無効。

修正案: `refreshSerialPorts()` のヒントから「t キーでテスト音」を削除する、または `t` キー処理を PORT_SELECT でも有効化する。

### 2.7 梅澤 Processing の seq 逆転処理が穴（5/27 指摘 2.7 継続）【重要度: Low】

`work/umezawa/hck/processing/processing.pde:152-162` で `packet.seq == partLastSeq` のみ「重複」扱い。`packet.seq < partLastSeq` のとき何もせず `lastSeqByPart[...] = packet.seq` で更新するため、その後 in-order な packet を「巨大ギャップ」扱いしてしまう。USB Serial 経由なので実害は低いが、`if (packet.seq <= partLastSeq) drop; return;` の方が一貫する。

### 2.8 梅澤 Processing の NoteOff 判定が `noteNumber == 0` まで含めていて過剰【重要度: Low】

5/27 指摘 2.8 と同じ。`work/umezawa/hck/processing/P05_SoundPartManager.pde:14` で `if (packet.gate == 0 || packet.velocity == 0 || packet.noteNumber == 0)` を NoteOff として扱う。`noteNumber == 0` は MIDI C-1（規格上は有効音）なので、過剰防御。

修正案: `noteNumber == 0` 条件を外し、`gate == 0` または `velocity == 0` のみで NoteOff とする。

### 2.9 `essentials/project.md` Mermaid 図ラベル「BEAT 2連送」と `architecture.md:139` の `beatRedundancy = 4` 記述が矛盾【重要度: Low】

指摘 1.3 のサブ項目だが、Mermaid 図のラベルは PNG 化されていないため初学者ページの図と本文の整合が直接ずれる。`docs/src/content/docs/essentials/project.md:62` の Mermaid edge label `CTRL 20Hz/<br/>BEAT 2連送` を `CTRL 20Hz/<br/>BEAT 4連送 (暫定)` に書き換える、または「N 連送」のように汎用化する。

---

## 3. 改善・新機能アイデア

### 3.1 楽器ノード共通ライブラリ集約と production 雛形整備をセットで実施【優先度: High / 工数: M】

指摘 2.3 + 2.4 + 2.5 を 1 PR で片付けると、ADR-0004 改訂後の「楽器 5 台 + 指揮 1 台」体制の足場が一気に整う。実機 3 台 + 指揮者で 1 回演奏確認できれば、production 移行時の差分が「楽譜と音色データのみ」に縮む。

### 3.2 確定値ロードマップ (`beatRedundancy` / `beatGapMs` / `SERIAL_DEBUG`) を progress.md に予約【優先度: High / 工数: S】

現在「暫定切り分け中」が 3 件並行しており、確定タイミングが定まらないと docs 横展開がループする。`progress.md` 末尾に「2026-05-30 までに `beatRedundancy=4 / beatGapMs=2` を実機計測し、結果で確定」のように計測予定日を入れると、レビューサイクルが収束する。

### 3.3 ADR-0004 改訂後の score_data 共通化【優先度: Med / 工数: M】

`firmware/test_v2/node_0{2,3,4}/src/score_data.cpp` は「3 台同一内容」前提で複製されている（`.agent/api.md:213`）。5 台に増えると複製コストが上がるうえ、間違って 1 台だけ書き換える事故が起きやすい。`firmware/test_v2/common/score/kirakira_boshi.cpp`（または `production/common/score/kaeru_no_uta.cpp`）にまとめて、各 node の `platformio.ini` の `lib_extra_dirs` で参照する形に変える。

### 3.4 課題曲「かえるのうた」score_data の起点ファイル作成【優先度: Med / 工数: M】

中⑦で曲が確定（`activeContext.md:54`）してから 6 日経過。`firmware/production/node_0{2,3,4,5}/src/score_data.cpp` のいずれかに「かえるのうた」起点ファイル（旋律 8 音、partId ごとの `headRestBeats` パターン）を草案として置いておくと、実装フェーズ突入時に意思決定の摩擦が減る。`.agent/progress.md` に予約タスクとして記録するだけでも良い。

### 3.5 `essentials/project.md` Mermaid キャプションを「N 連送」表記に統一【優先度: Low / 工数: S】

`docs/src/content/docs/essentials/project.md:62, 79` の「2 連送」表記を「複数連送（連送数は `beatRedundancy` で設定）」のように汎用化すると、確定値を変えるたびに広域置換せずに済む。同じ手法を `udp-multicast.md:46-48` や `troubleshooting.md:105` にも適用。

### 3.6 production CI matrix に「ビルドだけ通る空雛形」を入れて壊れに気付ける状態に【優先度: Med / 工数: S】

production が CI 対象外なので、`board` 名や `lib_extra_dirs` を間違えても気付かない。最低限 `void setup() {}` / `void loop() {}` だけの main で構わないので、CI matrix に乗せると 5/27 指摘 1.5 / 1.9 のような累積指摘を未然に防げる。

### 3.7 `.agent/conventions.md` ロギング節に「SERIAL_DEBUG=1 で push する場合は WIP 注記」のルール追加【優先度: Low / 工数: S】

5/25 の `ac8c5ff デバック` が 3 夜連続で残存指摘されているのは、コミットメッセージから意図が読めず巻き戻し判断ができないため。`.agent/conventions.md` に「`SERIAL_DEBUG=1` を main に残したまま push する場合、コミットメッセージに `[WIP]` 接頭辞と次の TODO を書く」運用ルールを追加すると、レビュー側が「これは恒久値か」を即判断できる。

---

## 4. 直近作業のディープレビュー（過去 7 日）

過去 7 日（5/21〜5/28）のコミットを `git log --since="7 days ago"` で取得。28 コミット中 11 件は夜間レビューレポートの cherry-pick（PR #9〜#17 をまとめてマージしたもの）で内容はレポート本体のみ。実体トピックは次の 3 件。

### トピック A — `e170ba7` で 2026-05-23〜27 累積指摘を一括追従（5/28・塩澤 + Claude Opus）

#### やったこと

- `e170ba7` (`[修正] 夜間レビュー累積指摘の追従 (2026-05-28) (#18)`、PR squash merge): 4 つの段階コミットを内包。
  1. ADR-0004 (楽器 5 台) を docs/firmware/pc_app コメントへ横展開（13 ファイル）。
  2. 架空モジュール名 `ScoreModule` / `NoteEmitterModule` を実体 `OrcReceiverModule` / `NoteSenderModule` に置換し、楽譜進行式 `beatNo - 1 - headRestBeats` の `-1` 補正欠落を `code/firmware.md` / `essentials/firmware.md` に追記。
  3. `OrcNetConfig.beatGapMs` 追加と `beatRedundancy = 2 → 4` 引上げを「2026-05-25 暫定切り分け中」として `docs/firmware/orc-net.md` / `docs/firmware/orc-sender.md` / `.agent/api.md` / `OrcSenderModule.h` 型コメントへ記載。
  4. 前段で取りこぼした ADR-0004 残存箇所（`OrcNetModule.h` WifiMode コメント / `pc_app/test_v2/README.md` / `architecture/three-stages.md` / `concept/goals.md` / `deep-dive/score-progression.md` / `deep-dive/udp-multicast.md` / `.agent/architecture.md`）と `beatRedundancy=4` の取りこぼし（`code/firmware.md` ProjectConfig.h サンプル / `essentials/firmware.md` / `orc-receiver.md` / `.agent/architecture.md`）を一括追従。

#### 完了度

- ADR-0004 横展開: docs/ 主要ページ＋ファーム公開ヘッダ＋pc_app README まで反映完了。残存（指摘 1.3）の「4 声」「2 連送」は補助節・古い数式の取りこぼし。
- `beatRedundancy=4` / `beatGapMs` 暫定値の記載: 「2026-05-25 暫定切り分け中」「実機計測で確定したら本節を書き直す」と明示しており、暫定状態を読者が誤解しない書き方になっている。
- `progress.md` への取り込み記録は未追記（5/25 で停止）。`activeContext.md` の「次の一手」も e170ba7 後の状態に更新されていない（指摘 1.6）。
- PR #18 の commit メッセージは詳細（各段階の対象ファイル列挙、docs ビルド 70 ページ・リンク切れなし確認まで明記）で品質が高い。

#### 設計判断

- 横展開を「コメント + ドキュメントのみ」に絞り、`firmware/.../OrcSenderModule.h:16` の型コメント変更（`(1-3)` → `(1-8 想定)`）以外はロジック不変としたのは CLAUDE.md「実機未テスト .ino/.cpp に Claude 起点で追加変更を入れない」ルールに準拠した妥当な判断。
- 「2026-05-25 暫定切り分け中」を明示する書き方は、確定値が変わっても本節だけ書き換えれば良い構造にできており、保守性が高い。
- 4 段階コミットを 1 PR で squash merge する判断は履歴の見通しを良くしており妥当。ただし PR 本文に各段階のコミット詳細をそのまま残しているので、後から「どの段階で何を変えたか」が PR ページから追跡可能。

#### 品質（コード系）

- ロジック変更なし。`OrcNetModule.cpp` の `beatGapMs` 反映（5/25 の `ac8c5ff` 由来）の `delay()` 呼び出しは未変更で、コメントのみ追加。
- docs ビルド 70 ページ・リンク切れなしは PR メッセージで確認済み。本レビュー時点でも `grep` で「4 声」「2 連送」の取りこぼしを確認できた（指摘 1.3）が、これは「優先順位の高い箇所のみ追従」とした PR スコープの問題で品質の問題ではない。

#### コンテンツ品質（doc / レポート）

- ADR-0004 改訂前後の歴史を `concept/goals.md` / `decisions/0005-firmware-embedded-module-architecture.md` で「5 台 → 6 台 (指揮 + 楽器)」と整理しており、初学者がページ間で迷いにくくなっている。
- `essentials/project.md:70-72` の脚注「production 想定では `node_05`（金管 4）と `node_06`（ドラム）を追加して 楽器 5 台構成」が ADR-0004 改訂を反映しており、Mermaid 図本体（test_v2 楽器 3 台版）と本文 production 5 台想定の使い分けが明確。
- 指摘 1.3 の「4 声」「2 連送」残存は補助節・拡張ガイドに集中しており、初学者の主動線（intro/overview → essentials → architecture）の文章は概ね整合。

#### 朝の人間レビューで確認したい質問

1. `progress.md` 末尾に 5/28 エントリ（PR #18 取り込み）を追記する作業は本日着手して良いか、それともユーザー側でまとめて書く運用か。
2. 指摘 1.3 の「4 声」「2 連送」取りこぼしを次の夜間追従コミットでまとめて消し込んでよいか（補助節・拡張ガイドのため優先度が低く、`e170ba7` のスコープ外として意図的に残した可能性もある）。
3. `OrcSenderModule.h:16` の型コメント変更は実機ビルド検証（`pio run`）を別途回したか。PR メッセージには docs ビルドのみ言及。

### トピック B — Processing サブシステム整備＆メインへのマージ（5/20〜5/27 / 梅澤）

5/27 レポートのトピック B と同じスコープ。`e170ba7` は梅澤 Processing に触れていないため、5/27 で挙げた問題（instrumentId 未読み、5 台未追随、ヒント矛盾、seq 逆転、NoteOff 過剰判定）はすべて継続中。本レビューでは重複を避け、変化があった点のみ記す。

#### やったこと（5/27 以降の差分）

- なし。`5eac1e5 Merge branch 'umezawa_work'` 以降、梅澤の Processing は変更なし。

#### 完了度

- 5/27 時点の状態のまま。ADR-0004 5 台対応の追従待ち（指摘 2.2）と `instrumentId` 対応（指摘 2.1）が最大の積み残し。

#### 朝の人間レビューで確認したい質問

1. 梅澤 Processing を「canonical の後継候補」として正式採用するか、「並列実装の試作版」として残すかの方針は決まったか。前者なら instrumentId / 5 台対応の優先度を High に上げる必要がある。
2. canonical (`pc_app/test_v2/orchestra_resynth/orchestra_resynth.pde`) との API 互換性試験（同一 NOTE フレームを両方で受信し挙動を突き合わせる）を実施する予定はあるか。

### トピック C — 作業ログテンプレート整備＆ファーム BEAT ロスト対策デバッグ（5/23〜5/28 / 塩澤）

5/27 レポートのトピック C と同じスコープ。`e170ba7` で docs 側の `beatRedundancy=4` / `beatGapMs` 記載は追従完了したが、`SERIAL_DEBUG=1` 非対称（指摘 1.1）は未解消。

#### やったこと（5/27 以降の差分）

- `e170ba7` で `OrcSenderModule.h:16` 型コメント `(1-3)` → `(1-8 想定)` への拡張と `OrcNetConfig.beatGapMs` の docs/.agent 反映が実施され、トピック C の「ドキュメント追従残」は概ね解消。

#### 完了度

- `beatGapMs` / `beatRedundancy=4` の docs 反映: 完了。
- `SERIAL_DEBUG=1` 非対称（node_02/03=1, node_04=0）: 未解消（指摘 1.1）。
- `ac8c5ff デバック` の意図記録: `progress.md` への追記なし。「暫定切り分け中」を明示した docs 追従とのトーンの差。

#### 朝の人間レビューで確認したい質問

1. `SERIAL_DEBUG=1` のまま push されている node_02 / node_03 を、いつ `0` に戻すか。本番演奏前の必須作業。
2. `beatRedundancy=4` / `beatGapMs=0` の実機計測（指摘 3.2）を予定しているか。確定値が決まると docs / api.md の「2026-05-25 暫定切り分け中」記述を書き直せる。
3. 作業ログテンプレ (`report/作業ログテンプレート/`) は配布原本との対応関係が docs/ 経由で辿れない。`docs/team/schedule.md` あたりに導線追加が必要か。

---

## 5. 次のアクション

朝の人間レビューで最優先で着手すべき 3 件。章 1〜4 を踏まえて優先度を決める。

### 5.1 `SERIAL_DEBUG=1` 非対称の解消と `progress.md` への意図記録【最優先 / 15 分】

**理由**: 現状 node_02 / node_03 だけ `SERIAL_DEBUG=1` のまま push されており、Processing 側が受信できる NoteFrame は node_04 のものだけ。本番演奏（または実機検証）すると 1/3 声欠落する。`e170ba7` の整合性追従が完了した今、最も実害の大きい残存問題。

対応:
- 演奏したいなら 3 ノードとも `0` に戻す（`firmware/test_v2/node_02/platformio.ini:33` / `node_03/platformio.ini:33`）。
- デバッグ継続なら `progress.md` に「2026-05-25 `ac8c5ff` で node_02/03 を `SERIAL_DEBUG=1` に切り分け中。本番演奏前に 0 へ戻す」と 1 行記録。
- 並行して `progress.md` に 5/28 エントリ（PR #18 取り込み）を追記、`activeContext.md` を e170ba7 後の状態に更新。

### 5.2 取りこぼし「4 声 / 2 連送」記述の追従と `essentials/project.md` Mermaid キャプション修正【高 / 1〜2 時間】

**理由**: `e170ba7` は主動線の docs を整合させたが、補助節 / 拡張ガイド / `pc-audio/` 章 / Mermaid キャプションが旧記述で残存（指摘 1.3 / 1.4 / 1.5 / 2.9）。次の夜間レビューでも同じ指摘が再浮上する。

対応（指摘 1.3 で列挙）:
- `docs/src/content/docs/essentials/project.md:62, 79` の「2 連送」表記を「N 連送（暫定 4）」または汎用化。
- `docs/src/content/docs/code/troubleshooting.md` / `firmware/orc-net.md` / `deep-dive/udp-multicast.md` / `deep-dive/score-progression.md` / `firmware/orc-sender.md` の「2 連送」を `beatRedundancy` 値参照に書き直し。
- `docs/src/content/docs/architecture/score.md:121` / `architecture/three-stages.md:64` / `decisions/0007-project-purpose-and-scope.md:86` の「4 声」を「5 声（ADR-0004 改訂）」に。
- `docs/src/content/docs/pc-audio/design.md:22, 51` / `signal-flow.md:126` の「3〜4 声」を「3〜5 声（test_v2 3 声・production 5 声目標）」に。

### 5.3 楽器ノード共通ライブラリ集約 + production CI 取り込み + `production/node_01` board 修正の 3 件セット【中 / 半日 + 実機ビルド検証 / 人間判断必須】

**理由**: いずれも 4 夜連続保留中（指摘 2.3 / 2.4 / 2.5）。3 件は連動しており、片付けると ADR-0004 改訂後の「楽器 5 台 + 指揮 1 台」体制の足場が一気に整う。実機ビルド検証が必要なため、CLAUDE.md ルールから Claude 単独では実行できない。

人間判断が必要なブロッカー:
- 実機 3 台（node_02/03/04）で `common/lib/OrcReceiverModule/` への集約後、`pio run -d firmware/test_v2/node_02` → `-t upload` → Processing で演奏確認。
- `firmware/production/node_01/platformio.ini` を `[env:seeed_xiao_esp32s3] / board = seeed_xiao_esp32s3 / platform = espressif32@6.10.0` に書き換え、`pio run -d firmware/production/node_01` でビルド通過確認。
- production matrix を `.github/workflows/pio-build.yml` に追加（node_01〜05 の 5 行、`node_06` は雛形を作って 6 行目に追加）。

---

### その他（人間判断ペンディング・本レビューでは扱わない）

- 課題曲「かえるのうた」の score_data 起点ファイル作成（指摘 3.4）— 中間発表後の本実装フェーズで着手。
- 梅澤 Processing の instrumentId / 5 台対応（指摘 2.1 / 2.2）— 梅澤本人作業。canonical との API 互換性試験を実施するなら方針確認が必要。
- `beatRedundancy` / `beatGapMs` 確定値の実機計測（指摘 3.2）— 塩澤判断、実機 1 台ずつ計測。
