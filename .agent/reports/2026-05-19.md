# 夜間レビューレポート 2026-05-19

対象ブランチ: `main`（最新コミット: `9597afc` 「追加」, 2026-05-19 22:56 JST）
レビュー実施: claude-opus-4-7 / 自動夜間枠

このレポートは「コードを直接変更しない」前提の差分監査。指摘の根拠は実体ファイルの
`path:line` 引用で示し、推測は「要調査」として明記する。

直近の大きな動きは **個人別中間発表 LaTeX の追加 3 件**（`ed1bb54`「中間発表追加」=
テンプレート + sty / `24a1a05`「追加」= 梅澤 plan_25G1021.tex / `9597afc`「追加」=
齋藤 plan_25G1053.tex）。複数メンバーが個人計画書を並行投入したことで、
**メンバー間で設計が食い違う / SSOT との乖離が再発する** という新しい監査軸が増えた。
昨日（2026-05-18）夜間レビューで指摘した 1.1-1.4/1.7/2.1 はすでに `029fef0` で
対応済みなので、本レポートでは新規追加分のみを観点に置く。

---

## 1. ドキュメント同期チェック

### 1.1 `plan_25G1053.tex` の node_01 役割が SSOT と完全に矛盾（High）

齋藤氏の中間発表計画書は、node_01 を **トランペット（楽器ノード）** として扱い、
指揮者は **「指揮棒用の別マイコン × 1」** として分離する独自アーキテクチャを
描いている：

- `report/計画書_中間発表/plan_25G1053.tex:75` :
  「5台のArduino楽器ノードが自分のパートの楽譜を保持し，指揮棒用の別マイコンから届く拍情報に合わせて」
- `report/計画書_中間発表/plan_25G1053.tex:137-143` : tikz 図で `(conductor) {指揮棒用\\別マイコン}` を
  `node_01〜node_05` の楽器群と並置
- `report/計画書_中間発表/plan_25G1053.tex:220-221` :
  「5台のArduino UNO R4 WiFiはすべて楽器ノードとして扱い，指揮棒は別マイコンへ分離する」
- `report/計画書_中間発表/plan_25G1053.tex:232` :
  `node\_01 & \code{0x01} & トランペット & 0拍 & 主旋律を曲頭から演奏する．`

しかし SSOT 側の確定事項は次のとおり：

- `.agent/architecture.md:6-13` : 「指揮者 1 + 楽器 3/4 の構成。指揮者ノードは XIAO ESP32-S3 Sense + GY-521」
- `.agent/architecture.md:91-97` : 「指揮者ノード（`firmware/test_v2/node_01/`）。ハードウェア:
  XIAO ESP32-S3 Sense + GY-521」「IMU から振りを検出 → 拍検出 → テンポ推定」
- `AGENTS.md:33-34` : 「指揮者: XIAO ESP32-S3 Sense + GY-521 / 楽器: Arduino UNO R4 WiFi」
- `docs/decisions/0003-imu-on-conductor-node.md`（ADR-0003、IMU を node_01 に統合）

つまり「指揮棒を別マイコンに切り出す」案は **ADR-0003 を覆す設計変更** に相当する。
中間発表でレビュアーがこの計画書だけ読むと、現行実装（node_01 = 指揮者）と齟齬を起こす。

**修正案**: 齋藤本人へ確認のうえ、次のいずれかを選択する。

- (A) 計画書を実装に合わせて書き直す（node_01 = 指揮者、node_02〜04 が楽器、partId 0x02-0x04）
- (B) 「指揮棒分離」が意図的な設計提案であれば、ADR-0007（新規）として記録し、ADR-0003 を deprecate する手続きを踏む

判断は user 必須。Claude 起点で計画書本文を書き換えない（本人の意図を勝手に上書きしないため）。

### 1.2 `plan_25G1053.tex` の partId 範囲 0x01〜0x05 が実体と乖離（High）

- `report/計画書_中間発表/plan_25G1053.tex:230-237` 表で
  `node\_01 ... 0x01 / node\_02 ... 0x02 / ... / node\_05 ... 0x05` を partId として割り当て
- `report/計画書_中間発表/plan_25G1053.tex:482` NOTE ペイロード表で
  `\code{0x01}〜\code{0x05}．発音元パート．`

実体は `0x01` を partId として使わない：

- `firmware/test_v2/common/lib/OrcProtocol/OrcProtocol.h:53` :
  `uint8_t partId; // test_v2 は 0x02-0x04 / production 想定は 0x02-0x05`
- `.agent/api.md:50` : 同併記
- 過去 3 夜の修正で `0x02-0x04 / 0x02-0x05 併記` 表現に統一済（5/17 1ad766b、5/18 029fef0）

`0x01` は実装上 `PKT_CTRL` の **PacketType 値** として既に使われている
（`OrcProtocol.h:22` : `PKT_CTRL = 1`）。partId と type の数値空間は別だが、
ドキュメント上「`0x01` を partId に充てる」と書かれると読者が混乱する。

**修正案**: 1.1 と同じく user 判断要。(A) 案を採るなら `0x02〜0x05` の併記表現に揃え、
node_01 の partId 行を削除。

### 1.3 `plan_25G1053.tex` の `ScoreEvent` が 5 フィールド版で実体 9 フィールドと乖離（Med）

- `report/計画書_中間発表/plan_25G1053.tex:338-355` :
  `beatAt / noteNumber / velocity / durationQ8 / flags` の 5 フィールド構造体宣言
- `report/計画書_中間発表/plan_25G1053.tex:430-440` 楽譜配列リテラル
  `{0, 60, 96, 256, 0x01}` で 5 要素

実体は 9 フィールド：

- `firmware/test_v2/node_02/include/score_data.h:20-32` :
  `beatAt / noteNumber / velocity / durationQ8 / flags / subNote / subVelocity / subOffsetQ8 / subDurationQ8`
- `.agent/api.md:191-201` : 同 9 フィールド版を正典として記載
- `.agent/architecture.md:159` : 5/18 修正済で 9 フィールド版に整合

齋藤の `ScoreEvent { ..., 256, 0x01 }` リテラルをそのまま実装に投入すると、
最後の `0x01` が `flags` に入って `bit0=NoteOn` だが、残りの `subNote/subVelocity/
subOffsetQ8/subDurationQ8` がゼロ初期化で済むので **コンパイル自体は通る**（C99 の
designated init を使わなくても残り 4 フィールドは 0 補完される）。ただし
「楽譜を 5 要素で書ける」という誤った理解が広まると、subNote を持つ細分音符が
書けない／読者が「9 要素必要」を見落とすリスクがある。

**修正案**: 計画書 5.2「楽譜データ形式」節を 9 フィールド版に改訂、または
「test_v2 は 5 フィールドで足りるが、実体は subNote 系まで含めて 9 フィールド」と
明示。1.1 / 1.2 とまとめて user → 齋藤確認の後で。

### 1.4 `plan_25G1053.tex` の楽器名 Tp/Hr/Tb/Tu/Dr と曲名「かえるのうた」が実体と乖離（Med）

- `report/計画書_中間発表/plan_25G1053.tex:221` : 「トランペット，ホルン，トロンボーン，チューバ，ドラム」
- `report/計画書_中間発表/plan_25G1053.tex:219` : 「課題曲候補は\textbf{「かえるのうた」}とする」
- `report/計画書_中間発表/plan_25G1053.tex:396-400` : `instrumentId 0=Tp / 1=Hr / 2=Tb / 3=Tu / 4=Dr`

実体は次のとおり：

- `pc_app/test_v2/orchestra_resynth/data/` 配下: `0_organ.json / 1_flute.json / 2_bell.json / 3_flute_tweaked.json`
- `.agent/architecture.md:104` : 「2026-05 時点は 0=オルガン / 1=フルート / 2=ベル / 3=フルート調整版」
- `.agent/architecture.md:6` : 「test_v2（現行・推奨）: きらきら星 3 声輪唱で稼働中」
- `firmware/test_v2/node_02/include/score_data.h:7` : 「『きらきら星 全曲』」

ただし `docs/src/content/docs/team/roles.md:96`「梅澤の役割『金管音色のサンプル合成試作』」と
`docs/.../team/schedule.md:54,79` は「金管」表記で残置（5/18 で「当時の役割分担として user 判断要」と保留）。
齋藤計画書の楽器セットも同様の **「計画段階での目標」vs「実装の現状」** の二重表記問題。

**修正案**: 1.1〜1.3 と一括で確認。「中間発表時点では Tp/Hr/Tb/Tu/Dr を最終目標とする
計画」「現行 test_v2 は organ/flute/bell の試作段階」と読める注釈を加えるのが穏便。

### 1.5 `plan_25G1021.tex` の NOTE ペイロード offset 18 が `reserved[2]` と誤記（High）

梅澤氏の Processing 計画書は NOTE パケットを次のように記述：

- `report/計画書_中間発表/plan_25G1021.tex:296` :
  `18 & reserved & uint8[2] & 現時点では未使用．`

これは **test_v2 の重要変更（instrumentId 追加）を見落としている**：

- `firmware/test_v2/common/lib/OrcProtocol/OrcProtocol.h:52-60` :
  `instrumentId` at offset 18, `reserved` at offset 19（合計 1 + 1 byte）
- `firmware/test_v2/common/lib/OrcProtocol/OrcProtocol.h:8-10` :
  「test_v2 の変更点: NotePayload の旧 reserved[0] を instrumentId に充てた」
- `.agent/api.md:50-56` : 同
- `pc_app/test_v2/orchestra_resynth/orchestra_resynth.pde:44` :
  `18 instrumentId uint8  0..N-1 (楽器番号 — data/*.json をファイル名昇順ソートしたときの index)`

梅澤計画書が `partId=0x02-0x05 → 金管/リズム` という固定対応で発音設計を組み立てている
（`plan_25G1021.tex:150-153, 327, 396-397`）のは、**`instrumentId` 概念を知らないで設計したから**と
思われる。実装側 `orchestra_resynth.pde` は instrumentId で音色を切り替える設計なので、
中間発表後にこの計画書通り Mac 4 台 / partId 固定金管音色を実装すると、
**現行 PC アプリの設計（instrumentId 切替）と接続できない**。

**修正案**: コード変更不要。`plan_25G1021.tex:286-298` の NOTE パケット表を以下に修正：

- 行 296 の `reserved` を 2 つに分割：`18 instrumentId uint8 PC 側 data/ をファイル名昇順
  ソートしたときの index` / `19 reserved uint8 0 埋め`
- 同章で `instrumentId` を扱う処理（Brass/Rhythm 切替のロジックを `partId` ではなく `instrumentId` で行う）を追記、または「test_v2 では partId と instrumentId が 1:1 対応なので
  partId だけで切替できる」と明記
- 梅澤本人の判断が要る部分は user 確認（本人が `instrumentId` を意図的に未採用とした可能性もあるため）

### 1.6 `plan_25G1021.tex` の「金管/Brass 1/2/3」が実体と乖離（Med）

- `report/計画書_中間発表/plan_25G1021.tex:82` : `126 & 金管音色の具体実装`
- `report/計画書_中間発表/plan_25G1021.tex:111` : `\texttt{0x02--0x04} は金管音色，\texttt{0x05} はリズム音色`
- `report/計画書_中間発表/plan_25G1021.tex:150-153` : 「Brass 1 / Brass 2 / Brass 3 / RhythmVoice」
- `report/計画書_中間発表/plan_25G1021.tex:326-328` : `\texttt{BrassVoice}`
- `report/計画書_中間発表/plan_25G1021.tex:341-376` : 「金管音色」+ 金管 ADSR テーブル

これは `docs/src/content/docs/team/roles.md:96` の梅澤役割「金管音色のサンプル合成試作」と
内的整合（梅澤の担当が「金管音色」のため）。**ただし実体は organ/flute/bell** で
進んでおり、中間発表時点で「これから金管に切り替える」と読むか「もう organ で進めてしまった」と
読むかで意味が変わる。

5/18 夜間レビュー指摘 1.2 と同根の問題。1.4 とまとめて整理が望ましい。

**修正案**: 5/18 と同じく user 判断要。「`team/roles.md` の梅澤役割は当時の分担として残す」
方針なら、本計画書も「金管音色を目標、現行は organ で試作」と注記する形が一貫する。

### 1.7 中間発表 LaTeX 3 件がいずれも PDF 未生成・未コミット（Med）

`report/計画書_中間発表/` 配下に PDF が一切無い：

```
hack-cover.sty / hack-fonts.sty / plan_25G1021.tex / plan_25G1053.tex / plan_template.tex
```

CLAUDE.md / `.agent/conventions.md` の LaTeX 運用規約と乖離：

- `.agent/conventions.md:108-118` :
  「`.tex` の変更と PDF の更新は**同一コミット**にまとめる（分けない）」
  「Docker 環境を持たないメンバーが提出物 PDF を確認できないと提出・レビューが止まる」
- `.agent/conventions.md:108` :
  「コンパイルが通らないままコミットするのは禁止」

3 件とも `% !TEX program = lualatex` 指定（`plan_25G1021.tex:1` / `plan_25G1053.tex:1` /
`plan_template.tex:1`）なので、Docker ではなく **LuaLaTeX ローカル**でコンパイルする必要があり、
かつ Hiragino フォントが必要な環境がある。Claude のリモート環境ではコンパイル検証不可。

**修正案**: 各執筆者が手元で `latexmk -lualatex main.tex` 相当を実行し、生成 PDF を
別コミットでまとめて push。なお自分以外の .tex を勝手にコンパイルすると同じ作業が
重複するので、執筆者ごとに自分の plan_25G\*.tex のみを担当する運用が望ましい。

### 1.8 `plan_25G1021.tex` の node_05 = リズム と `plan_25G1053.tex` の node_05 = ドラム の概念ずれ（Low）

両計画書で node_05 / partId 0x05 の役割が微妙に異なる：

- `report/計画書_中間発表/plan_25G1021.tex:153` :
  `node\_05 & 0x05 & ベース・リズム伴奏 & RhythmVoice．短い減衰を持つリズム音`
- `report/計画書_中間発表/plan_25G1053.tex:236` :
  `node\_05 & \code{0x05} & ドラム & 0拍 & 1拍ごとのリズム伴奏を演奏する．`

「リズム伴奏」自体は一致するが、Umezawa は Kick/Snare/Hi-hat を noteNumber で振り分ける詳細
（`plan_25G1021.tex:392-396`）まで設計しており、齋藤の「ドラム」は単純な 1 拍ごと打音と
読める。本番までに突き合わせれば済む程度の差。

**修正案**: 中間発表後に「production 想定の node_05 は Kick/Snare/Hi-hat 切替（梅澤設計）」で
統一する旨を `.agent/architecture.md` の本番想定節に 1 行で明記する案を残しておく
（コード作業はまだ要らない）。

---

## 2. バグ・矛盾レビュー

### 2.1 個人計画書 2 件で設計が根本的に矛盾している（High）

1.1〜1.5 を横断する構造的問題。表で整理：

| 観点 | plan_25G1021.tex（梅澤） | plan_25G1053.tex（齋藤） | SSOT（実体） |
|---|---|---|---|
| node_01 の役割 | 指揮者 | トランペット（楽器） | 指揮者 |
| 指揮者の実装 | node_01 内に統合 | 別マイコン × 1（外出し） | node_01 内に統合 |
| partId 範囲 | 0x02-0x05（4 パート） | 0x01-0x05（5 パート） | 0x02-0x04（test_v2）/ 0x02-0x05（production 想定） |
| 楽器セット | Brass×3 + Rhythm | Tp/Hr/Tb/Tu/Dr の 5 種 | organ/flute/bell（現行）|
| ScoreEvent | 言及なし（PC 側設計のみ）| 5 フィールド版（架空） | 9 フィールド版 |
| instrumentId | reserved として未活用 | 採用（NOTE offset 18） | 採用（NOTE offset 18） |
| 曲 | 言及なし | かえるのうた | きらきら星 |

**中間発表時点でレビュアー（教員）が両方を見ると、チームの設計方針が定まっていないと
判定されるリスクがある**。役割分担で章をまたいで担当が異なるのは普通だが、
**「指揮者をどこに置くか」「partId 体系」「楽譜形式」のような基底設計はチームで
合意して 1 案に絞っておくべき**。

**修正案**: user 主導で齋藤・梅澤を交えて「現行 test_v2 を踏まえた中間発表用の
チーム共通アーキテクチャ章」（1〜2 ページ）を 1 本書き、各個人計画書はその章を
引用したうえで担当範囲だけ詳細化する形に整理する。これが間に合わない場合は最低限、
「計画書 vs 現行実装の差分」を巻末に併記する。

### 2.2 「指揮棒用マイコン分離」が ADR-0003 を覆す設計変更だが ADR が無い（High）

`plan_25G1053.tex:75 / 137 / 220-221` は指揮棒を node_01 から切り出すと
記述しているが、`docs/decisions/0003-imu-on-conductor-node.md`（=「IMU を node_01 に統合」）の
撤回・置換手続きを経ていない。要 user 判断：

- (A) 齋藤計画書を **撤回**（実装通り node_01 = 指揮者）
- (B) ADR-0007（新規）「指揮棒を node_06 として分離する」を起票し、ADR-0003 を deprecate

**修正案**: 中間発表前に意思決定。プロジェクト時間軸（発表 7/1）を考えると、
追加マイコン購入・実装やり直しの余地は少ないので (A) が現実的。

### 2.3 `production/node_01/platformio.ini` が `board = uno_r4_wifi` のまま（Med、**4 夜連続保留**）

`firmware/production/node_01/platformio.ini:14-17` :

```ini
[env:uno_r4_wifi]
platform = renesas-ra
board = uno_r4_wifi
framework = arduino
```

`.agent/activeContext.md` でも 3 夜連続保留と明記されている指摘の、4 夜目の継続。CI が
production/ を build マトリクスに入れていないため、production 雛形が壊れたまま
通り抜けている根本原因はそのまま。

**修正案**: 過去 3 夜と同じ。`board = seeed_xiao_esp32s3` / `platform = espressif32@6.10.0` /
`upload_protocol = esp-builtin` に変更し、`pio run -d firmware/production/node_01` を
user が手元で 1 回回す。Claude のリモート環境では PlatformIO ツールチェインが
無いので実機側で完結する作業。

### 2.4 `plan_25G1053.tex` の楽譜配列リテラルが実体 ScoreEvent と要素数不一致（Med）

`plan_25G1053.tex:430-440` の `kScore[] PROGMEM = { {0, 60, 96, 256, 0x01}, ... }` は
5 要素のリテラル。実体 `ScoreEvent` は 9 フィールドなので、コードそのままを
新規ノード（`production/node_06` 等を作るなら）の `score_data.cpp` に貼ると、
**残り 4 フィールドが 0 で補完される**（C++ aggregate-initialization）。
コンパイル自体は通るが `subDurationQ8 = 0` などの不要な細分音符スロットが
明示されないまま 0 になるため、後で誰かが追加修正したときに見落とす危険がある。

**修正案**: 1.3 の修正に合わせて、リテラルを `{0, 60, 96, 256, 0x01, 0, 0, 0, 0}` のように
9 要素に揃えるか、`{.beatAt=0, .noteNumber=60, ...}` の designated init で書く。
これも user → 齋藤確認が必要。

### 2.5 「追加」だけのコミットメッセージが規約違反（Low）

過去 7 日で 2 件：

- `24a1a05` (Umezawa 2026-05-19 03:02) 「追加」 → plan_25G1021.tex 513 行追加
- `9597afc` (Saito 2026-05-19 22:56) 「追加」 → plan_25G1053.tex 627 行追加

`.agent/conventions.md:75-83` :

| 種別 | 用途 |
|---|---|
| `[ドキュメント]` | ドキュメントのみの変更 |

> 機能単位で細かく分割（無関係な変更を 1 コミットに混ぜない）

`[追加]` または `[ドキュメント]` で始めるルール。同じく過去ログにも
`miyo` 氏の自動メッセージ風 `Implement feature X to enhance user experience...`
（`d39b4e2` / `e7ee2d3` / `4a104a3` 等）が並んでおり、規約遵守がチーム内で
徹底されていない印象がある。

**修正案**: 規約違反コミットを書き換える必要は無い（履歴を汚さない方が良い）。
次のコミットから守るよう、`CONTRIBUTING.md` 末尾 or `.agent/conventions.md` 冒頭に
具体例を 2-3 行で補強する案を残しておく。Claude 側で書き換えるのは Med-Low 優先度。

### 2.6 `presentation/rink` の正体不明（Low、要調査）

`1d11877`「rinkファイルを新規作成．パワポのリンクがこれなので，編集できなかったらいって」と
`fd7eb18` で `rink` が追加されているが、内容 1 行・拡張子なし。
過去レポートでも未触れ。プレゼン用バイナリリンク（OneDrive 等？）として
意図的に置いている可能性が高い。

**修正案**: `presentation/README.md` に「rink ファイルは PPT へのリンク。直接編集不可」と
1 行追記し、新規参加者が混乱しないようにする。優先度低、必要なら無視可。

### 2.7 重複設計の構造的根因（5/18 から継続 / Med-High）

5/18 で指摘した「`node_03/04/src/score_data.cpp` のヘッダコメント sed 過誤」
（既に `029fef0` で修正済）は表面的な症状で、本質は
「3 ノード分の `score_data.cpp` を手で揃える運用が必ず壊れる」点。
`.agent/activeContext.md:既知の論点` でも触れている。
コード変更を伴うので Claude 側で着手しないが、根治策の候補は 2 つ：

- (a) `tools/sync_score.sh`（1 ファイルから 3 つに同期）
- (b) `firmware/test_v2/common/lib/` への `score_data.cpp` 共通化 + ノード固有 `headRestBeats` だけ差分化

実機テスト要のため当面保留。後述の章 3.4 で改善案として再掲。

### 2.8 セキュリティ・認可関連

`.agent/conventions.md` レビューと前回までと同じく、SoftAP `OrchestraAP / orchestra2026` の
平文埋め込み（`.agent/api.md:121-126`、5/18 指摘 2.5 と同じ）は意図的設計のため
継続して許容。新規セキュリティ懸念は本日無し。

---

## 3. 改善・新機能アイデア

### 3.1 中間発表用「チーム共通アーキテクチャ章」を 1 本書く

- なぜ: 個人計画書 2 件で node_01 役割・partId・楽譜形式・楽器セットがバラバラ。
  発表時にレビュアーが整合性を疑う。共通章を引用する形にすれば、個人計画書間の
  揺れを最小化できる
- 工数: M（既存 `.agent/architecture.md` を LaTeX 化するだけなら 2-3 時間）
- 優先度: 高（章 1.1 / 1.2 / 2.1 / 2.2 の根治策）

### 3.2 plan_*.tex 用 CI ジョブ（LuaLaTeX）

- なぜ: 章 1.7 のとおり PDF 未コミット問題が複数人で同時発生した。CI で
  `latexmk -lualatex` を回せば、push 前にコンパイル不能を検知できる。
  ただし Hiragino フォント依存の場合は CI 環境で代替フォントへのフォールバック設定が必要
- 工数: M（GitHub Actions で `texlive-luatex` セットアップ + Noto 系へのフォント代替）
- 優先度: 中（中間発表後で良い、ただし本番計画書も同じ構成なら早めに）

### 3.3 `score_data` の共通化（`common/lib/` 化 or sync スクリプト）

- なぜ: 章 2.7 / 5/18 指摘 2.1 / 5/17 指摘 2.1 と 3 夜連続の構造問題。
  ヘッダコメントの sed 過誤は症状で、本質は 3 ファイル手動同期。
  実機テスト要なので Claude 起点で着手しない
- 工数: M（共通化）/ S（sync スクリプト）
- 優先度: 中（中間発表後に着手、production 移行と合わせて）

### 3.4 `NoteSender` / `OrcReceiver` の `common/lib/` 集約

- なぜ: 5/15 から保留中（5/17 / 5/18 でも継続指摘）。`firmware/test_v2/node_0{2,3,4}/src/`
  配下に同じモジュールが 3 重複しているため、3.3 の score_data 共通化と組み合わせて
  根治。実機テスト要のため Claude 着手不可
- 工数: M-L
- 優先度: 中（production 整備フェーズの起点になる作業）

### 3.5 `presentation/rink` の正体メモを `presentation/README.md` に 1 行

- なぜ: 章 2.6 のとおり、新規メンバーが見るとファイル形式が不明で混乱する。
  PPT 元データへの URL ショートカットだとわかる注釈で十分
- 工数: S（5 分）
- 優先度: 低（新規メンバーの onboarding を考えるとあれば便利）

### 3.6 `production/` を CI ビルドマトリクスに含める or 「雛形は CI 対象外」を明文化

- なぜ: 章 2.3 が **4 夜連続保留** になっている根本理由が「CI が production を見ていない」点。
  二択：(a) CI に含めて `pio run -d firmware/production/*` を実行（雛形が壊れていたら
  CI が落ちる）、(b) `.github/workflows/pio-build.yml` に「production は雛形のため対象外」と
  明示しレビュー対象から外す
- 工数: S（CI YAML 編集 5 行 + 動作確認）
- 優先度: 中（雛形のままだと「次のフェーズで誰かが必ず引っかかる」リスクがある）

---

## 4. 直近作業のディープレビュー（過去 7 日）

過去 7 日の git log（`git log --since="7 days ago" --no-merges`）から、ユーザーが集中して
取り組んでいたトピックを 3 件抽出した：

### トピック A: **中間発表用 個人計画書の追加 3 件**（`ed1bb54` / `24a1a05` / `9597afc`）

- **やったこと**:
  - 2026-05-18 22:10 (TakumiShiozawa, `ed1bb54`) 「中間発表追加」:
    `report/計画書_中間発表/` に `plan_template.tex`（193 行）・`hack-cover.sty`・`hack-fonts.sty` を
    追加、同時に `AGENTS.md` の `.agent/` インポート方針を変更（`@import` 廃止 → バックティック参照）
  - 2026-05-19 03:02 (Umezawa, `24a1a05`) 「追加」:
    `plan_25G1021.tex`（513 行）追加。Processing 音源サブシステムの基本設計・詳細設計
  - 2026-05-19 22:56 (Shota Saito, `9597afc`) 「追加」:
    `plan_25G1053.tex`（627 行）追加。楽譜データ形式・5パート振り分け・音色データ連携仕様
- **完了度**:
  - 計画書本文は両方とも未コンパイル・PDF 不在（章 1.7）
  - TODO / FIXME / WIP マーカーは本文に無し
  - コミットメッセージが規約違反「追加」のみ（章 2.5）
  - 計画書同士で設計が矛盾（章 2.1）が **未解消** で発表予定（5/20 ？）に向かっている
- **設計判断**:
  - **梅澤計画書**: 「PC 側 4 Mac 並列」+「partId 固定の Brass×3 + Rhythm」+「instrumentId 未活用」は、現行 `orchestra_resynth.pde`（1 Mac で全 partId を受信、instrumentId で音色切替）と
    アーキテクチャが大きく異なる。**シンプルさを取ったトレードオフ**だが、実装移行コストが
    膨大になる懸念
  - **齋藤計画書**: 「指揮棒を別マイコンに分離」+「node_01 を楽器化」+「partId 0x01-0x05」は
    ADR-0003 を覆す設計変更。`docs/decisions/0003-imu-on-conductor-node.md` のリビジョン手続きが
    無いまま個人計画書だけが先行している
  - **より素直な選択肢**: 現行 test_v2 アーキテクチャ（node_01 = 指揮者、node_02-04 = 楽器、
    instrumentId で音色切替）に揃えて、個人計画書はその上の「PC 側設計」「楽譜進行設計」を
    詳述するだけにする方が圧倒的に少ない労力で済む
- **品質（コード系）**: コード変更なし
- **コンテンツ品質（doc / レポート）**:
  - 両計画書とも章立て・表組み・図表（`plan_25G1053.tex` は TikZ あり）は丁寧
  - 用語の揺れ・引用の正確性: SSOT との乖離が複数（章 1.1-1.6）
  - 文章の論理破綻はないが、両者を並べると **チームとして合意していない設計を 2 案
    並行投入している** 構図に見える
  - Markdown 構造の崩れ: なし（LaTeX なので別問題系）
- **朝の人間レビューで確認したい質問**:
  1. 齋藤計画書の「指揮棒を別マイコンに分離」は意図的な提案か、それとも齋藤が現行実装の
     ADR-0003 を知らずに書いたものか？ → 後者なら、実装に合わせて書き直しを依頼
  2. 梅澤計画書の「partId 固定で金管音色」は中間発表向けの整理であり、本番実装は
     `instrumentId` で切り替える前提で良いか？ → 計画書の章末に「実装は instrumentId 駆動」
     と注釈追加で済む
  3. 中間発表は 5/20 か？（`docs/.../team/schedule.md:79` 周辺で「5/20 プレゼン」と
     書かれている記憶がある） → 修正の時間があるかの判断軸

### トピック B: **夜間レビュー対応サイクルの定着**（5/15 → 5/19 で 4 連続）

- **やったこと**:
  - 2026-05-17 17:27 (`1ad766b`) 5/15 夜間レビュー指摘 1.1-1.7/1.9 を 1 コミット一括反映
  - 2026-05-17 17:29 (`ffac733`) `.agent/activeContext.md` を上書き
  - 2026-05-18 09:17 (`bc8cdea`) 5/17 夜間レビュー指摘 1.2-1.8 を 1 コミット一括反映
  - 2026-05-18 09:17 (`38605ce`) `.agent/activeContext.md` を上書き
  - 2026-05-19 11:08 (`029fef0`) 5/18 夜間レビュー指摘 1.1-1.4/1.7/2.1 を 1 コミット一括反映
  - 2026-05-19 11:10 (`6645b8f`) `.agent/activeContext.md` を上書き
  - レポート本体は別途 `cherry-pick` で main 取り込み（`316c5b0` / `0808e52`）
- **完了度**: サイクル自体は 3 連続でうまく回っている。指摘ごとの High / Med / Low 分類と
  自走可能 / user 判断要の切り分けが定着し、Claude 起点で着手しない領域
  （実機テスト要、方針判断要、PDF 生成要）も明確に保留している
- **設計判断**:
  - 「cherry-pick で main 取り込み → 残存修正を 1 コミットで → activeContext を上書き」の
    3 ステップ運用は安定している
  - **トレードオフ**: PR を立てるたびに sha スナップショットが古くなる問題（5/18 で
    `24a1a05` 時点を見ていた）は構造的に解決不能。毎朝 sha 照合する運用案（5/18 で
    `conventions.md` への運用追記候補に挙がっていた）は **未着手**
- **品質（コード系）**: docs ビルドは毎回検証（70 ページ生成、リンク切れなし）
- **コンテンツ品質**: `progress.md` のエントリが長文化（5/18 / 5/19 で 6〜8 行）。
  ナビゲーション性は若干落ちるが、5/18 で導入した「指摘 N.M のうち XX 件を対応」明示は
  検索性に貢献している
- **朝の人間レビューで確認したい質問**:
  1. 夜間レビューサイクルを「中間発表まで」継続するか、「production 整備フェーズ」に
     入ったら一時停止するか？ 計画書が出揃った後の検査軸が変わる可能性がある
  2. 4 夜連続保留の `production/node_01 platformio.ini` を user が手元で 1 回回すタイミングは
     いつになるか？（中間発表前 / 後）

### トピック C: **AGENTS.md のインポート方針変更**（`ed1bb54` の AGENTS.md 部分）

- **やったこと**: `ed1bb54` の中で AGENTS.md の `.agent/` インポート表記を
  `@.agent/architecture.md` → `` `.agent/architecture.md` `` に変更。
  「`@import` で常時展開すると毎ターン約 550 行をロードしてトークンを浪費するため、
  各仕様書はバックティック参照に留め、必要なときに Read で開く運用」と明示
- **完了度**: AGENTS.md 中の参照 3 箇所（architecture / conventions / api）が
  バックティック参照に統一済み。`activeContext.md` / `progress.md` の `@` プレフィックスは
  そのまま（毎ターン更新が必要なので残す設計判断）
- **設計判断**: トークン節約と「タスクに直接関係するファイルだけ Read する」運用への
  方針転換。確かに 550 行 × 3 ファイルを毎ターン読むのは無駄。
  ただし **副作用**: AI エージェントがコンテキストを Read し忘れると、SSOT を見ずに
  作業を始めるリスクが上がる。今夜のレポート作成（このレポート）でも Read を明示的に
  追加して回避した
- **品質（コード系）**: なし
- **コンテンツ品質**: AGENTS.md の指針節（line 86-90）に「読み込み指針」が新規追加され、
  「拍検出を調整するなら architecture.md の拍検出節と api.md の logic_params 表」のように
  具体例が示されている。読みやすさは向上
- **朝の人間レビューで確認したい質問**:
  1. このインポート方針変更後、Claude が SSOT を Read し忘れて誤情報をベースに作業する
     インシデントが起きていないか？（既に起きていたら、運用文書での Read 強制 / hooks 等を
     検討）

---

## 5. 次のアクション

朝の人間レビューで最優先で着手すべき 3 件：

### アクション 1: 齋藤・梅澤計画書のチーム間整合確認（**user 判断必須・最優先**）

- 章 1.1 / 1.2 / 1.5 / 2.1 / 2.2 の根治
- 判断軸:
  - (Q1) **齋藤計画書 (`plan_25G1053.tex`) の node_01 = トランペット / 指揮棒分離は意図的か？**
    - **意図的（ADR-0003 を覆す）**: ADR-0007 を起票し、テンプレート移行のロードマップを引く
    - **意図的でない（実装非認知）**: 計画書を実装に合わせて書き直し依頼（最低限 node_01 役割と partId 範囲）
  - (Q2) **梅澤計画書 (`plan_25G1021.tex`) の `instrumentId` 未採用は意図的か？**
    - **意図的**: 中間発表は partId 固定で進め、本番で instrumentId 採用と明記
    - **意図的でない**: 計画書 NOTE 表（offset 18-19 行）を `instrumentId / reserved` に修正
- ブロッカー: 本人の意図を確認しないと自走不可

### アクション 2: 計画書 PDF コンパイル + コミット（**user 作業・優先度 高**）

- 章 1.7（CLAUDE.md / `.agent/conventions.md` 違反の解消）
- 担当・手順: 各執筆者が自分の `plan_25G\*.tex` を手元 LuaLaTeX でコンパイルし、
  生成 PDF を **同一コミット** で追加（過去の慣行どおり）。テンプレート (`plan_template.tex`) は
  塩澤側で 1 度コンパイルすれば足る
- ブロッカー: Hiragino フォント環境（user の手元 Mac）
- Claude 補助可能範囲: コミットメッセージ案 `[ドキュメント] 中間発表 plan_25G1021 PDF
  生成（梅澤）` 程度

### アクション 3: `plan_25G1021.tex` の NOTE オフセット表のみ修正（**自走可・中規模 1 コミット**）

- 章 1.5 の修正案（コード変更ゼロ・本人意図確認不要の最小修正）
- 内容: `plan_25G1021.tex:296` の 1 行
  「`18 & reserved & uint8[2] & 現時点では未使用．`」を 2 行に分割
  - `18 & instrumentId & uint8 & PC 側 data/*.json をファイル名昇順ソートしたときの index．`
  - `19 & reserved & uint8 & 0 で埋める．`
- ただし 1.5 後半（partId 固定金管 / instrumentId 駆動の整合）は user 判断要なので、
  オフセット表の事実誤認だけ修正
- **条件付き保留**: 「梅澤本人の意図的選択」だった場合は逆に「本実装では offset 18 に
  instrumentId が入っているが本計画書では PC 側の判断で無視する設計」を 1 行で
  注釈する方が筋。**user 確認後**着手が安全

人間判断が必要なブロッカー（再掲）:

- (B1) 齋藤計画書の指揮棒分離: ADR-0003 撤回（追加マイコン投入）or 計画書書き直しの二択
- (B2) 梅澤計画書の partId 固定金管 / instrumentId 採否: 計画書 5 章相当を全面的に書き直すかの判断
- (B3) `production/node_01/platformio.ini` の board 変更（4 夜連続保留）: 手元 `pio run` 必須
- (B4) 中間発表（5/20 想定？）までの修正可能時間: B1 / B2 のタイムボックス決定

---

## 付録: 今回 Read したファイル一覧（再現性のため）

- `report/計画書_中間発表/plan_25G1021.tex`（梅澤・513 行・PDF なし）
- `report/計画書_中間発表/plan_25G1053.tex`（齋藤・627 行・PDF なし）
- `report/計画書_中間発表/plan_template.tex`（塩澤・193 行・PDF なし）
- `firmware/test_v2/common/lib/OrcProtocol/OrcProtocol.h`
- `firmware/test_v2/node_02/include/score_data.h`
- `firmware/production/node_01/platformio.ini`
- `pc_app/test_v2/orchestra_resynth/orchestra_resynth.pde`（ヘッダコメントのみ）
- `pc_app/test_v2/orchestra_resynth/data/`（ファイル名のみ）
- `.agent/architecture.md` / `.agent/api.md` / `.agent/conventions.md`
- `.agent/activeContext.md` / `.agent/progress.md`
- `.agent/reports/2026-05-18.md`（前夜レポート、形式参照）
- `AGENTS.md`
- git log `--since="7 days ago"`
